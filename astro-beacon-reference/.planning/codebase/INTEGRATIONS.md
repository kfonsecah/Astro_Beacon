# External Integrations

**Analysis Date:** 2026-04-01

## APIs & External Services

**None detected.** This project is a **visual reference only** — a static web application that renders mock UI screens for the Astro Beacon concept. No external API calls, fetch requests, or HTTP clients are present in the codebase.

- No `fetch()` calls found in `src/`
- No `axios` or HTTP client libraries in dependencies
- No Supabase, Firebase, AWS, or any backend SDK imports
- No API route handlers or server-side code

## Data Storage

**Databases:**
- None — all data is hardcoded/mock data within page components

**File Storage:**
- Local filesystem only — static assets served from `public/` directory

**Caching:**
- TanStack React Query is configured as a provider in `src/App.tsx` but no queries are defined — no active caching layer

## Authentication & Identity

**Auth Provider:**
- None implemented — `src/pages/Login.tsx` is a visual mock screen only, no authentication logic

## Monitoring & Observability

**Error Tracking:**
- None

**Logs:**
- None — no logging framework configured

## CI/CD & Deployment

**Hosting:**
- Lovable platform (default per `README.md`) — static SPA deployment
- Can be deployed to any static hosting (Vercel, Netlify, GitHub Pages, etc.)

**CI Pipeline:**
- None configured — no `.github/workflows/` or CI config files detected

## Environment Configuration

**Required env vars:**
- None — project has no `.env` files and requires no environment variables

**Secrets location:**
- N/A — no secrets used

## Webhooks & Callbacks

**Incoming:**
- None

**Outgoing:**
- None

## Fonts & External Resources

**Google Fonts:**
- IBM Plex Mono (weights: 300, 400, 500, 600)
- Space Grotesk (weights: 300, 400, 500, 600, 700)
- Loaded via CSS `@import` in `src/index.css` from `fonts.googleapis.com`

## Lovable Platform Integration

**Lovable Services:**
- `lovable-tagger` 1.1.13 — Dev-time component tagging plugin (`vite.config.ts`)
- `lovable-agent-playwright-config` — Playwright E2E test configuration wrapper (`playwright.config.ts`, `playwright-fixture.ts`)
- Project generated by Lovable (IA) — code changes can be pushed back to Lovable platform per README

---

*Integration audit: 2026-04-01*
