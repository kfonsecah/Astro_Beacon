---
phase: 22
plan: 01
status: draft
nyquist_compliant: false
wave: 1
---

# Phase 22, Plan 01: Cleanup & Documentation Sincronization

## Objective
Eliminate dead code (`token-refresh.ts` and `auth.context.tsx`), remove the development artifact button in the dashboard, and synchronize `REQUIREMENTS.md` and `STATE.md` to accurately reflect the completion of Milestone v1.2.

## Verification & Nyquist
*This phase is a non-functional code cleanup. Validation will run simple builds to ensure no imports are broken.*

## Implementation Steps

### 1. Verification of Dead Code
<threat_model>
- **Threat**: Accidental deletion of in-use code.
- **Mitigation**: Using `grep_search` to verify `token-refresh.ts` and `auth.context.tsx` have zero active imports before executing removal.
</threat_model>

<task>
**Action**: Verify `token-refresh` and `auth.context` imports.
**Command**: `grep_search` on `src/` and `app/` directories.
</task>

### 2. Remove Dead Files
<threat_model>
- **Threat**: Leftover artifacts break the build.
- **Mitigation**: Delete both files entirely.
</threat_model>

<task>
**Action**: Delete `src/services/token-refresh.ts`.
**Action**: Delete `src/context/auth.context.tsx`.
</task>

### 3. Remove UAT Button from Dashboard
<threat_model>
- **Threat**: Users access unpolished development views in production.
- **Mitigation**: Hard removal of the `{/* UAT: Reanimated Test */}` block from `dashboard.tsx`.
</threat_model>

<task>
**File**: `app/(tabs)/dashboard.tsx`
**Action**: Locate the `TouchableOpacity` block that routes to `/reanimated-test` (around line 155-161) and remove it entirely using `multi_replace_file_content`.
</task>

### 4. Build Verification
<threat_model>
- **Threat**: The application fails to compile after the deletions.
- **Mitigation**: Run the TypeScript compiler check.
</threat_model>

<task>
**Action**: Run `npx tsc --noEmit` to ensure there are no missing module errors.
<automated>
Command: `npx tsc --noEmit`
Expected: Exits with 0 (no TS errors).
</automated>
</task>

### 5. Update Documentation
<threat_model>
- **Threat**: Documentation desync leads to duplicated work or inaccurate tracking.
- **Mitigation**: Manually update checkboxes and counters.
</threat_model>

<task>
**File**: `.planning/REQUIREMENTS.md`
**Action**: Change `- [ ]` to `- [x]` for AUTH-01, 05, 06, 07, 08, API-02, 03, 05, UI-04, 06, 08, 10, 12. Fix the Traceability table so ERR-01 through ERR-06 point to `Phase 21`. Update the Last updated comment.

**File**: `.planning/STATE.md`
**Action**: Update completed plans/phases counts, mark Phase 22 as completed, and update timestamps.
</task>

## Verification
- Project compiles without errors.
- Requirements checkboxes represent reality.
- Dev buttons no longer exist.
