---
phase: 06-refactorizacion-de-estilos
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/(tabs)/bestiary.tsx
  - app/(tabs)/map.tsx
autonomous: true
requirements:
  - "Diseño, animaciones y estilos temáticos - Requerimientos Generales"

must_haves:
  truths:
    - "Bestiary screen has zero hardcoded hex colors in StyleSheet"
    - "Map screen has zero hardcoded hex colors in StyleSheet"
    - "Classification and danger colors come from constants, not inline maps"
    - "Supply drop status colors come from constants, not inline maps"
    - "Both screens respond to theme changes (dark/light toggle)"
    - "Card component used for list items instead of inline card styles"
    - "Badge component used for classification and status labels"
  artifacts:
    - path: "app/(tabs)/bestiary.tsx"
      provides: "Bestiary with theme-aware styles and UI components"
      min_lines: 80
    - path: "app/(tabs)/map.tsx"
      provides: "Map with theme-aware styles and UI components"
      min_lines: 100
  key_links:
    - from: "app/(tabs)/bestiary.tsx"
      to: "src/constants/colors.ts"
      via: "classification and danger color constants"
      pattern: "classificationAnimal|dangerAmigable"
    - from: "app/(tabs)/map.tsx"
      to: "src/constants/colors.ts"
      via: "supply status color constants"
      pattern: "supplyPendiente|supplyEntregado"
---

<objective>
Refactor bestiary and map screens to eliminate hardcoded styles and use theme + UI components.

Purpose: These 2 screens have unique styling patterns (classification colors, danger levels, supply status colors) that need to use the constants system instead of inline color maps.
Output: Two screens fully themed, using Card, Badge components, and color constants.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/(tabs)/bestiary.tsx
@app/(tabs)/map.tsx
@src/components/ui/Card.tsx
@src/components/ui/Badge.tsx
@src/constants/colors.ts
@src/constants/spacing.ts
@src/constants/typography.ts
@src/hooks/use-theme.ts
@src/theme/index.ts

<interfaces>
From src/constants/colors.ts (classification colors):
```typescript
classificationAnimal: '#3B82F6'
classificationPlanta: '#22C55E'
classificationRecurso: '#6EE7B7'
classificationMicroorganismo: '#A855F7'
classificationDesconocido: '#6B7280'
classificationOtro: '#F59E0B'
```

From src/constants/colors.ts (danger levels):
```typescript
dangerAmigable: '#22C55E'
dangerCauteloso: '#F59E0B'
dangerPeligroso: '#EF4444'
dangerLetal: '#A855F7'
```

From src/constants/colors.ts (supply drop status):
```typescript
supplyPendiente: '#F59E0B'
supplyEntregado: '#22C55E'
supplyRecogido: '#6B7280'
supplyExpirado: '#EF4444'
```

From src/components/ui/Card.tsx:
```typescript
export function Card(props: { children: React.ReactNode; style?: ViewStyle; accent?: boolean }): JSX.Element
```

From src/components/ui/Badge.tsx:
```typescript
export function Badge(props: { label: string; variant?: 'default' | 'success' | 'warning' | 'danger' | 'info'; color?: string; style?: ViewStyle; textStyle?: TextStyle }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor bestiary.tsx to use theme, color constants, and UI components</name>
  <files>app/(tabs)/bestiary.tsx</files>
  <action>
    1. Add imports: `import { useTheme } from '@/hooks/use-theme'`, `import { spacing, typography, colors } from '@/constants'`, `import { Card, Badge } from '@/components/ui'`
    2. Add `const theme = useTheme()` inside the component
    3. DELETE the inline `classificationColors` and `dangerColors` maps — replace with `colors` constants:
       - Map species.classification to colors constants: `planta` → `colors.classificationPlanta`, `animal` → `colors.classificationAnimal`, `recurso` → `colors.classificationRecurso`, `microorganismo` → `colors.classificationMicroorganismo`, `desconocido` → `colors.classificationDesconocido`, `otro` → `colors.classificationOtro`
       - Map species.danger to colors constants: `amigable` → `colors.dangerAmigable`, `cauteloso` → `colors.dangerCauteloso`, `peligroso` → `colors.dangerPeligroso`, `letal` → `colors.dangerLetal`
    4. Replace species cards with `<Card>` component wrapping each species row
    5. Replace classification badge inline styles with `<Badge>` component using `color` prop with the resolved classification color from constants
    6. Replace danger dot inline colors with the resolved danger color from constants
    7. Replace all hardcoded hex values:
       - `#0B1120` → `theme.colors.background`
       - `#111827` → `theme.colors.surface`
       - `#1F2937` → `theme.colors.border`
       - `#6EE7B7` → `theme.colors.primary`
       - `#E5E7EB` → `theme.colors.text`
       - `#9CA3AF` → `theme.colors.textSecondary`
       - `#6B7280` → `theme.colors.textMuted`
       - `#4B5563` → `theme.colors.textMuted`
       - `#374151` → `theme.colors.textDisabled`
       - `#22C55E` → `colors.classificationPlanta` (or theme.colors.success)
       - `#3B82F6` → `colors.classificationAnimal` (or theme.colors.info)
       - `#A855F7` → `colors.classificationMicroorganismo`
       - `#F59E0B` → `colors.classificationOtro` (or theme.colors.warning)
    8. Replace RefreshControl tintColor with `theme.colors.primary`
    9. Replace all hardcoded typography with `typography` constants
    10. Replace all hardcoded spacing with `spacing` constants
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <done>Bestiary compiles without errors, uses Card for species rows, Badge for classification labels, colors constants for classification/danger colors, theme colors for all other styling, zero inline color maps, zero hardcoded hex values</done>
</task>

<task type="auto">
  <name>Task 2: Refactor map.tsx to use theme, color constants, and UI components</name>
  <files>app/(tabs)/map.tsx</files>
  <action>
    1. Add imports: `import { useTheme } from '@/hooks/use-theme'`, `import { spacing, typography, colors } from '@/constants'`, `import { Card, Badge } from '@/components/ui'`
    2. Add `const theme = useTheme()` inside the component
    3. DELETE the inline `statusColors` map — replace with `colors` constants:
       - `pendiente` → `colors.supplyPendiente`
       - `entregado` → `colors.supplyEntregado`
       - `recogido` → `colors.supplyRecogido`
       - `expirado` → `colors.supplyExpirado`
    4. Replace supply cards with `<Card>` component wrapping each supply row
    5. Replace status badge inline styles with `<Badge>` component using `color` prop with the resolved status color from constants
    6. Replace map placeholder hardcoded styles:
       - Background: `theme.colors.surface`
       - Border: `theme.colors.border`
       - Center dot: `theme.colors.primary`
       - Center dot border: `theme.colors.background`
       - Center label: `theme.colors.primary`
       - Marker labels: `theme.colors.textSecondary`
       - Scan line: `theme.colors.textDisabled`
    7. Replace all hardcoded hex values:
       - `#0B1120` → `theme.colors.background`
       - `#111827` → `theme.colors.surface`
       - `#1F2937` → `theme.colors.border`
       - `#6EE7B7` → `theme.colors.primary`
       - `#E5E7EB` → `theme.colors.text`
       - `#9CA3AF` → `theme.colors.textSecondary`
       - `#6B7280` → `theme.colors.textMuted`
       - `#4B5563` → `theme.colors.textMuted`
       - `#F59E0B` → `colors.supplyPendiente` (or theme.colors.warning)
       - `#22C55E` → `colors.supplyEntregado` (or theme.colors.success)
    8. Replace all hardcoded typography with `typography` constants
    9. Replace all hardcoded spacing with `spacing` constants
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <done>Map compiles without errors, uses Card for supply rows, Badge for status labels, colors constants for supply status colors, theme colors for map placeholder styling, zero inline color maps, zero hardcoded hex values</done>
</task>

</tasks>

<verification>
Both screens compile without TypeScript errors. Grep for hardcoded hex patterns returns zero matches in bestiary.tsx and map.tsx:
```bash
rg '#[0-9A-Fa-f]{6}' app/\(tabs\)/bestiary.tsx app/\(tabs\)/map.tsx
```
Should return no matches (or only comments).

Verify no inline color maps remain:
```bash
rg 'classificationColors|dangerColors|statusColors' app/\(tabs\)/
```
Should return no matches.
</verification>

<success_criteria>
- 0 hardcoded hex colors in bestiary.tsx and map.tsx
- 0 inline color maps (classificationColors, dangerColors, statusColors deleted)
- Colors constants used for classification, danger, and supply status colors
- Card component replaces inline card styles in both screens
- Badge component used for classification and status labels
- All screens use useTheme() hook for color resolution
- All typography uses typography constants
- All spacing uses spacing constants
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-refactorizacion-de-estilos/06-03-SUMMARY.md`
</output>
