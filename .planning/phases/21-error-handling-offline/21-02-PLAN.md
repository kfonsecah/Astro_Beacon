---
phase: 21-error-handling-offline
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/(tabs)/_layout.tsx
  - src/utils/queryClient.ts
autonomous: true
requirements: [ERR-03, ERR-04, ERR-06]
must_haves:
  truths:
    - "Queries pause when offline and retry automatically when online"
    - "An offline banner is visible across all tabs when offline"
  artifacts:
    - path: "app/(tabs)/_layout.tsx"
      provides: "OfflineBanner conditional rendering"
    - path: "src/utils/queryClient.ts"
      provides: "NetInfo to onlineManager bridge"
  key_links:
    - from: "queryClient"
      to: "NetInfo"
      via: "onlineManager.setEventListener"
---

<objective>
Implement global offline support via TanStack Query's onlineManager and display a persistent offline banner.

Purpose: Provide a deterministic offline experience where queries pause safely and the user is informed.
Output: Integrated `NetInfo` with `onlineManager` and conditionally rendered `OfflineBanner`.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-error-handling-offline/21-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect NetInfo to onlineManager</name>
  <files>src/utils/queryClient.ts</files>
  <action>Import `NetInfo` from `@react-native-community/netinfo` and `onlineManager` from `@tanstack/react-query`. Configure the `onlineManager.setEventListener` bridging `NetInfo.addEventListener`. Make sure to return the cleanup function to prevent memory leaks. (Per D-09, D-10 from CONTEXT.md).</action>
  <verify>grep_search `onlineManager.setEventListener` in `src/utils/queryClient.ts` finds the setup</verify>
  <done>onlineManager is connected to NetInfo</done>
</task>

<task type="auto">
  <name>Task 2: Render OfflineBanner in Tabs Layout</name>
  <files>app/(tabs)/_layout.tsx</files>
  <action>Import `useNetworkStatus` hook and `OfflineBanner` component. Render `<OfflineBanner />` conditionally before the `<Tabs>` component (both wrapped in `<View style={{ flex: 1 }}>`) only when `isConnected` is false. (Per D-06, D-07 from CONTEXT.md).</action>
  <verify>grep_search `OfflineBanner` in `app/(tabs)/_layout.tsx` returns a match</verify>
  <done>OfflineBanner is rendered conditionally based on network status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device→App | Native network status affecting app logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Denial of Service | queryClient | accept | Device network drops simply pause queries, handled safely by react-query |
</threat_model>

<verification>
Check `queryClient.ts` for proper `onlineManager` setup and `app/(tabs)/_layout.tsx` for `OfflineBanner`.
</verification>

<success_criteria>
- `onlineManager` uses `NetInfo` to pause and resume queries
- `OfflineBanner` shows up above the tabs when offline
</success_criteria>

<output>
After completion, create `.planning/phases/21-error-handling-offline/21-02-SUMMARY.md`
</output>
