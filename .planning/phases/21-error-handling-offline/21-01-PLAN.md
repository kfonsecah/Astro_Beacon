---
phase: 21-error-handling-offline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/RouteErrorFallback.tsx
  - src/components/common/index.ts
  - app/_layout.tsx
  - app/(auth)/_layout.tsx
  - app/(auth)/login.tsx
  - app/(tabs)/dashboard.tsx
  - app/(tabs)/resources.tsx
  - app/(tabs)/bestiary.tsx
  - app/(tabs)/logbook.tsx
  - app/(tabs)/map.tsx
  - app/trips.tsx
autonomous: true
requirements: [ERR-01, ERR-02]
must_haves:
  truths:
    - "A crash in any screen does not crash the entire app"
    - "User sees a graceful HUD-style error message with a retry button"
  artifacts:
    - path: "src/components/common/RouteErrorFallback.tsx"
      provides: "Reusable error UI"
  key_links:
    - from: "app routes"
      to: "RouteErrorFallback"
      via: "export function ErrorBoundary"
---

<objective>
Implement ErrorBoundary pattern across all routes for graceful crash recovery.

Purpose: Prevent unhandled JS errors from taking down the React Native app.
Output: A reusable RouteErrorFallback component and ErrorBoundary exports in all routes.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-error-handling-offline/21-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RouteErrorFallback component</name>
  <files>src/components/common/RouteErrorFallback.tsx, src/components/common/index.ts</files>
  <action>Create `RouteErrorFallback` component using the HUD design system. It must accept `{ error: Error; retry: () => void }` props. Use `tc.background` for the container background, `tc.danger` for the error text ("ERROR DEL SISTEMA"), `tc.textMuted` for the `error.message`, and a button with `tc.primary` border labeled "[ REINTENTAR ]". Use monospace fonts. Export it from `index.ts`. (Per D-02, D-03, D-05 from CONTEXT.md).</action>
  <verify>grep_search `RouteErrorFallback` in `src/components/common/index.ts` finds the export</verify>
  <done>Component exists and is exported via common index</done>
</task>

<task type="auto">
  <name>Task 2: Inject ErrorBoundary into all routes</name>
  <files>
    app/_layout.tsx,
    app/(auth)/_layout.tsx,
    app/(auth)/login.tsx,
    app/(tabs)/dashboard.tsx,
    app/(tabs)/resources.tsx,
    app/(tabs)/bestiary.tsx,
    app/(tabs)/logbook.tsx,
    app/(tabs)/map.tsx,
    app/trips.tsx
  </files>
  <action>In each route file, import `RouteErrorFallback` from `@/components/common` and add exactly this named export: `export function ErrorBoundary({ error, retry }: { error: Error; retry: () => void }) { return <RouteErrorFallback error={error} retry={retry} />; }`. (Per D-01, D-04 from CONTEXT.md).</action>
  <verify>grep_search `export function ErrorBoundary` in `app/_layout.tsx` returns a match</verify>
  <done>All specified route files export the standard ErrorBoundary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component→User | Error messages displayed to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | RouteErrorFallback | accept | `error.message` is standard JS Error output, doesn't expose sensitive tokens since the backend sanitizes responses |
</threat_model>

<verification>
Check that all route files have the ErrorBoundary export and index.ts exports RouteErrorFallback.
</verification>

<success_criteria>
- RouteErrorFallback component built using HUD theme values without hardcoded hex colors
- app/_layout.tsx and all requested screen routes export ErrorBoundary
</success_criteria>

<output>
After completion, create `.planning/phases/21-error-handling-offline/21-01-SUMMARY.md`
</output>
