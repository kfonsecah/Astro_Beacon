---
phase: 10-endpoints-de-dominio
plan: 05
type: execute
wave: 5
depends_on:
  - 10-04
files_modified:
  - api/
autonomous: false
requirements:
  - RES-01
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - LBK-01
  - LBK-02
  - ASTR-01
  - ASTR-02
  - ASTR-03
  - TRIP-01
  - TRIP-02
  - TRIP-03
  - TRIP-04
  - TRIP-05
  - SUPP-01
  - SUPP-02
  - SUPP-03
  - SUPP-04
  - SYNC-01
---

<objective>
Final verification of all Phase 10 domain endpoints including API tests and manual verification.
</objective>

<context>
@api/src/app.ts — Mounted routes from 10-04
@.planning/REQUIREMENTS.md — All requirements for Phase 10
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run integration tests for all domain endpoints</name>
  <files>
    - api/
  </files>
  <action>
Create a test script to verify all domain endpoints:

1. Start the server: `npm run dev` (or `npm start`)
2. Register a test user:
   - POST /api/v1/auth/register with { email, password }
3. Login to get tokens:
   - POST /api/v1/auth/login with { email, password }
   - Extract accessToken

Test each domain endpoint using curl or a test script:

**Resources:**
- POST /api/v1/resources — Create resource
- GET /api/v1/resources — List resources (check pagination)
- GET /api/v1/resources/:id — Get single resource
- PUT /api/v1/resources/:id — Update resource
- POST /api/v1/resources/:id/movements — Record movement
- GET /api/v1/resources/alerts — Get alerts
- DELETE /api/v1/resources/:id — Delete resource

**Species:**
- POST /api/v1/species — Create species
- GET /api/v1/species — List species (with filters)
- GET /api/v1/species/:id — Get single species
- PUT /api/v1/species/:id — Update species
- DELETE /api/v1/species/:id — Delete species

**Logbook:**
- POST /api/v1/logbook — Create entry
- GET /api/v1/logbook — List entries
- GET /api/v1/logbook/:id — Get single entry
- PUT /api/v1/logbook/:id — Update entry
- DELETE /api/v1/logbook/:id — Delete entry

**Astronaut:**
- GET /api/v1/astronaut — Get profile (should create on first access or return 404)
- PUT /api/v1/astronaut — Update profile
- GET /api/v1/astronaut/stats — Get dashboard stats

**Trips:**
- POST /api/v1/trips — Create trip
- GET /api/v1/trips — List trips
- GET /api/v1/trips/:id — Get single trip
- PUT /api/v1/trips/:id — Update trip
- POST /api/v1/trips/:id/start — Start trip
- POST /api/v1/trips/:id/end — End trip (completado)
- POST /api/v1/trips/:id/o2/adjustment — Add O2 adjustment
- DELETE /api/v1/trips/:id — Delete trip

**Supplies:**
- POST /api/v1/supplies — Create supply
- GET /api/v1/supplies — List supplies
- GET /api/v1/supplies/nearby?lat=X&lng=Y&radius=1000 — Nearby search
- GET /api/v1/supplies/:id — Get single supply
- PUT /api/v1/supplies/:id — Update supply
- POST /api/v1/supplies/:id/collect — Collect supply
- DELETE /api/v1/supplies/:id — Delete supply

**Auth verification:**
- Try accessing domain endpoint without token → expect 401
- Try accessing domain endpoint with invalid token → expect 401

Create output file: `api/TEST_RESULTS.md` with results.
  </action>
  <verify>
All endpoints respond with 2xx status codes and return expected data structure
  </verify>
  <done>
Integration tests pass for all domain endpoints
</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Manual verification of key features</name>
  <what-built>
Complete Phase 10 domain endpoints with:
- Resources CRUD + movements + alerts
- Species CRUD + filtering
- Logbook CRUD + species linking
- Astronaut profile + dashboard stats
- Trips lifecycle + O2 tracking
- Supplies with GPS nearby search
</what-built>
  <how-to-verify>
1. Start server: `cd api && npm run dev`
2. Register new account in app or via curl
3. Test these critical flows:

**Resource Alert Flow:**
- Create resource with category 'oxigeno' and low amount (e.g., 5%)
- GET /api/v1/resources/alerts → should show the resource

**Trip O2 Tracking:**
- Create trip with plannedDuration
- Start trip → status changes to 'activo'
- Wait 1 second (or test O2 calculation directly)
- End trip → status changes to 'completado', O2Consumed calculated

**Supply Nearby:**
- Create supplies at different locations
- GET /api/v1/supplies/nearby?lat=0&lng=0&radius=1000000 → should return all

**Unauthorized Access:**
- Request without Authorization header → 401 Unauthorized

4. Check `api/TEST_RESULTS.md` for automated test results
  </how-to-verify>
  <resume-signal>
Type "approved" or describe any issues found
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run build passes without errors
- [ ] npm run dev starts server successfully
- [ ] All domain endpoints respond correctly
- [ ] Authentication middleware blocks unauthorized requests
- [ ] Pagination works on list endpoints
- [ ] lastModified timestamps present in responses
</verification>

<success_criteria>
All Phase 10 requirements verified. 22 domain requirements + 1 sync requirement = 23 total requirements implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/10-endpoints-de-dominio/10-05-SUMMARY.md`
</output>
