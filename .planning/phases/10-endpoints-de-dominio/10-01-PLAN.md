---
phase: 10-endpoints-de-dominio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/models/resource.model.ts
  - api/src/models/species.model.ts
  - api/src/models/logbook.model.ts
  - api/src/models/astronaut.model.ts
  - api/src/models/trip.model.ts
  - api/src/models/supply.model.ts
  - api/src/schemas/resource.schema.ts
  - api/src/schemas/species.schema.ts
  - api/src/schemas/logbook.schema.ts
  - api/src/schemas/astronaut.schema.ts
  - api/src/schemas/trip.schema.ts
  - api/src/schemas/supply.schema.ts
autonomous: true
requirements:
  - RES-01
  - SPEC-01
  - LBK-01
  - ASTR-01
  - TRIP-01
  - SUPP-01
  - SYNC-01
---

<objective>
Create all domain models and Zod validation schemas for Phase 10 entities: Resources, Species, Logbook, Astronaut, Trips, Supplies.
</objective>

<context>
@api/src/models/user.model.ts — Pattern for Mongoose models with timestamps
@api/src/schemas/auth.schema.ts — Pattern for Zod schemas
@src/types-dtos/enums.ts — Frontend enum definitions to match

### Frontend Enums to Match
```typescript
ResourceCategory: 'oxigeno' | 'agua' | 'comida' | 'medico' | 'equipo' | 'otro'
SpeciesClassification: 'animal' | 'planta' | 'recurso' | 'microorganismo' | 'desconocido' | 'otro'
DangerLevel: 'amigable' | 'cauteloso' | 'peligroso' | 'letal'
TripStatus: 'planificado' | 'activo' | 'completado' | 'abortado'
SupplyDropStatus: 'pendiente' | 'entregado' | 'recogido' | 'expirado'
MovementType: 'ingreso' | 'egreso'
GeoPoint: { lat: number, lng: number }
```

### Default Thresholds (D-03)
- oxigeno: 20%
- agua: 15%
- comida: 10%
- medico, equipo, otro: no default threshold
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Resource models (Resource + ResourceMovement)</name>
  <files>
    - api/src/models/resource.model.ts
  </files>
  <action>
Create Mongoose models for resources with embedded movement history:

**Resource model:**
- name: string (required)
- category: enum ['oxigeno', 'agua', 'comida', 'medico', 'equipo', 'otro'] (D-01)
- currentAmount: number (default 0)
- unit: string (e.g., 'litros', 'unidades', 'kg')
- threshold: number (optional, default based on category: O2=20%, Water=15%, Food=10%) (D-03, D-04)
- userId: ObjectId ref User (required, index)
- lastModified: Date (for offline sync) (D-19, SYNC-01)
- timestamps: true (createdAt, updatedAt)

**ResourceMovement model** (embedded or separate):
- type: enum ['ingreso', 'egreso'] (D-02)
- amount: number
- notes: string (optional)
- timestamp: Date (default now)
- previousAmount: number
- newAmount: number

Follow User model pattern with timestamps: true and proper indexes.
  </action>
  <verify>
TypeScript compiles without errors, and all models export I{ModelName} interfaces
  </verify>
  <done>
Resource and ResourceMovement models exist with proper enums, thresholds, and timestamps
</done>
</task>

<task type="auto">
  <name>Task 2: Create Species model</name>
  <files>
    - api/src/models/species.model.ts
  </files>
  <action>
Create Mongoose Species model:

- name: string (required)
- classification: enum ['animal', 'planta', 'recurso', 'microorganismo', 'desconocido', 'otro'] (D-06)
- dangerLevel: enum ['amigable', 'cauteloso', 'peligroso', 'letal'] (D-07)
- description: string (optional)
- imageUrl: string (optional)
- notes: string (optional)
- userId: ObjectId ref User (required, index)
- lastModified: Date (for offline sync) (D-19, SYNC-01)
- timestamps: true

Index userId for efficient queries. Include SPEC-01, SPEC-03.
  </action>
  <verify>
TypeScript compiles without errors, Species model exports ISpecies interface
  </verify>
  <done>
Species model exists with classification and dangerLevel enums
</done>
</task>

<task type="auto">
  <name>Task 3: Create Logbook model</name>
  <files>
    - api/src/models/logbook.model.ts
  </files>
  <action>
Create Mongoose LogbookEntry model:

- title: string (required)
- description: string (optional)
- photoUrl: string (optional) (LBK-01)
- location: GeoPoint { lat: number, lng: number } (optional)
- speciesId: ObjectId ref Species (optional) (LBK-02)
- userId: ObjectId ref User (required, index)
- lastModified: Date (for offline sync) (D-19, SYNC-01)
- timestamps: true

Note: TTS narration (LBK-03) is deferred to Phase 12 per D-10.
  </action>
  <verify>
TypeScript compiles without errors, LogbookEntry model exports ILogbookEntry interface
  </verify>
  <done>
LogbookEntry model exists with photo URL and optional species linking
</done>
</task>

<task type="auto">
  <name>Task 4: Create Astronaut model</name>
  <files>
    - api/src/models/astronaut.model.ts
  </files>
  <action>
Create Mongoose Astronaut model (D-11):

- userId: ObjectId ref User (required, unique, index)
- name: string (required)
- status: enum ['activo', 'inactivo', 'emergencia'] (matches AstronautStatus)
- birthDate: Date (optional)
- specializations: string[] (optional, e.g., ['biología', 'medicina'])
- lastModified: Date (for offline sync) (D-19, SYNC-01)
- timestamps: true

One-to-one relationship with User. Status enum from frontend.
  </action>
  <verify>
TypeScript compiles without errors, Astronaut model exports IAstronaut interface
  </verify>
  <done>
Astronaut model exists with one-to-one User relationship and status enum
</done>
</task>

<task type="auto">
  <name>Task 5: Create Trip model</name>
  <files>
    - api/src/models/trip.model.ts
  </files>
  <action>
Create Mongoose Trip model (D-13, D-14, D-15):

- destination: string (optional)
- status: enum ['planificado', 'activo', 'completado', 'abortado'] (D-13)
- startDate: Date (optional)
- endDate: Date (optional)
- plannedDuration: number (hours, optional)
- O2Config: object (D-14):
  - baseRate: number (default 1 unit/hour)
  - manualAdjustments: array of { amount: number, note: string, timestamp: Date }
- O2Consumed: number (calculated from baseRate + manual)
- userId: ObjectId ref User (required, index)
- lastModified: Date (for offline sync) (D-19, SYNC-01)
- timestamps: true

Status transitions: planificado → activo (start) → completado/abortado (end)
  </action>
  <verify>
TypeScript compiles without errors, Trip model exports ITrip interface with O2Config
  </verify>
  <done>
Trip model exists with status workflow and hybrid O2 tracking
</done>
</task>

<task type="auto">
  <name>Task 6: Create Supply model</name>
  <files>
    - api/src/models/supply.model.ts
  </files>
  <action>
Create Mongoose Supply model (D-16, D-17):

- name: string (required)
- description: string (optional)
- status: enum ['pendiente', 'entregado', 'recogido', 'expirado'] (D-16)
- location: GeoPoint { lat: number, lng: number } (D-17)
- contents: string[] (optional, e.g., ['oxigeno', 'comida', 'medicina'])
- deliveredAt: Date (optional)
- collectedAt: Date (optional)
- expiresAt: Date (optional)
- userId: ObjectId ref User (required, index)
- lastModified: Date (for offline sync) (D-19, SYNC-01)
- timestamps: true

Create 2dsphere index on location for geospatial queries (SUPP-03: nearby).
  </action>
  <verify>
TypeScript compiles without errors, Supply model exports ISupply interface with location index
  </verify>
  <done>
Supply model exists with GPS location and 2dsphere index for geospatial queries
</done>
</task>

<task type="auto">
  <name>Task 7: Create all Zod validation schemas</name>
  <files>
    - api/src/schemas/resource.schema.ts
    - api/src/schemas/species.schema.ts
    - api/src/schemas/logbook.schema.ts
    - api/src/schemas/astronaut.schema.ts
    - api/src/schemas/trip.schema.ts
    - api/src/schemas/supply.schema.ts
  </files>
  <action>
Create Zod schemas matching models (follow auth.schema.ts pattern):

**resource.schema.ts:**
- createResourceSchema: name, category, currentAmount?, unit?, threshold?
- updateResourceSchema: partial of create
- movementSchema: type (ingreso/egreso), amount, notes?

**species.schema.ts:**
- createSpeciesSchema: name, classification, dangerLevel?, description?, imageUrl?, notes?
- updateSpeciesSchema: partial of create

**logbook.schema.ts:**
- createLogbookSchema: title, description?, photoUrl?, location?, speciesId?
- updateLogbookSchema: partial of create

**astronaut.schema.ts:**
- createAstronautSchema: name, status?, birthDate?, specializations?
- updateAstronautSchema: partial of create

**trip.schema.ts:**
- createTripSchema: destination?, plannedDuration?, O2Config?
- updateTripSchema: status?, startDate?, endDate?, O2Consumed?, manualAdjustments?
- startTripSchema: { } (empty, status transition)
- endTripSchema: { status: 'completado' | 'abortado' }

**supply.schema.ts:**
- createSupplySchema: name, description?, status?, location, contents?, expiresAt?
- updateSupplySchema: partial of create including status transition
- nearbyQuerySchema: lat, lng, radius (meters)

Export types: TypeNameInput = z.infer<typeof schema>
  </action>
  <verify>
npm run build passes without TypeScript errors
  </verify>
  <done>
All Zod schemas exist with proper validation, enums match frontend
</done>
</task>

</tasks>

<verification>
- [ ] All 6 Mongoose models created with timestamps and lastModified
- [ ] All 6 Zod schemas created with matching enums
- [ ] TypeScript compiles without errors
- [ ] Models export I{ModelName} interfaces
- [ ] Schemas export TypeNameInput types
</verification>

<success_criteria>
All domain models and schemas exist and compile. Ready for service layer implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-endpoints-de-dominio/10-01-SUMMARY.md`
</output>
