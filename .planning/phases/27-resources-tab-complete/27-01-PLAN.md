---
phase: 27-resources-tab-complete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types-dtos/recurso.dto.ts
  - src/theme/dark.ts
  - src/theme/light.ts
  - app/(tabs)/resources.tsx
autonomous: true
requirements: [UI-04, DOM-03]

must_haves:
  truths:
    - "Recurso interface includes maxCapacity?: number field."
    - "darkTheme and lightTheme expose warningMuted and warningBorder color tokens."
    - "ProgressBar in resources.tsx receives correct max (item.maxCapacity fallback) and thresholdPercentage."
    - "Movement history renders inside each resource card (renderItem), not after the FlatList."
    - "Alert section uses tc.warningMuted and tc.warningBorder — zero hardcoded rgba strings."
    - "keyExtractor uses item.id ?? `resource-${index}` with no unsafe casts."
  artifacts:
    - path: "src/types-dtos/recurso.dto.ts"
      provides: "Recurso type with maxCapacity field"
    - path: "src/theme/dark.ts"
      provides: "warningMuted and warningBorder tokens in darkTheme"
    - path: "src/theme/light.ts"
      provides: "warningMuted and warningBorder tokens in lightTheme"
    - path: "app/(tabs)/resources.tsx"
      provides: "Correct ProgressBar values + inline movement history + design system compliance"
  key_links:
    - from: "app/(tabs)/resources.tsx"
      to: "27-CONTEXT.md"
      via: "Bugs B-01, B-02, B-03, B-04, B-05"
    - from: "src/types-dtos/recurso.dto.ts"
      to: "docs/DATA-DESIGN.md"
      via: "capacidadMaxima field §2.2"
---

<objective>
Corregir los bugs críticos del tab de Recursos que producen datos incorrectos en la barra de progreso y
rompen el renderizado del historial de movimientos.

Purpose: Que el tab de Recursos muestre datos reales y correctos conectados a la API.
Output:
- DTO Recurso con maxCapacity.
- Tema extendido con tokens warningMuted / warningBorder.
- resources.tsx con ProgressBar correcto, historial dentro de las cards, colores del design system.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-resources-tab-complete/27-CONTEXT.md
@src/types-dtos/recurso.dto.ts
@src/theme/dark.ts
@src/theme/light.ts
@src/constants/colors.ts
@app/(tabs)/resources.tsx
@src/components/ui/ProgressBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir maxCapacity al DTO Recurso</name>
  <files>src/types-dtos/recurso.dto.ts</files>
  <action>
    - Añadir `maxCapacity?: number` a la interface `Recurso`, después del campo `currentAmount`.
    - El campo es opcional para compatibilidad con respuestas de API que aún no lo incluyan.
    - No modificar ninguna otra interface del archivo.
  </action>
</task>

<task type="auto">
  <name>Task 2: Extender darkTheme con tokens warningMuted y warningBorder</name>
  <files>src/theme/dark.ts</files>
  <action>
    - En `darkTheme.colors`, añadir:
      ```ts
      warningMuted: colors.warningMuted,
      warningBorder: colors.warningBorder,
      ```
    - Añadirlos después de la entrada `warning: colors.warning` para mantener agrupación semántica.
    - Importar `colors` desde `@/constants/colors` si aún no está importado (actualmente SÍ está importado).
  </action>
</task>

<task type="auto">
  <name>Task 3: Extender lightTheme con tokens warningMuted y warningBorder</name>
  <files>src/theme/light.ts</files>
  <action>
    - Mismo cambio que Task 2 pero en `lightTheme`.
    - Leer el archivo primero para ver la estructura exacta.
    - Añadir `warningMuted` y `warningBorder` con los mismos valores (colores semánticos, no dependen del modo).
  </action>
</task>

<task type="auto">
  <name>Task 4: Corregir resources.tsx — bugs B-01, B-02, B-03, B-05 + mover historial dentro de renderItem</name>
  <files>app/(tabs)/resources.tsx</files>
  <action>
    1. **keyExtractor (B-05):** Reemplazar la expresión actual con `(item, index) => item.id ?? \`resource-\${index}\``.

    2. **Sección de alertas (B-03):** En `ListHeaderComponent`, reemplazar los strings `rgba(251,146,60,0.1)` y `rgba(251,146,60,0.3)` con `tc.warningMuted` y `tc.warningBorder` respectivamente.

    3. **renderItem completo (B-01 + B-02 + historial):** Reescribir el renderItem para:
       a. Calcular `max` correctamente:
          ```ts
          const max = item.maxCapacity ?? (item.threshold ? Math.round(item.threshold / 0.15) : 100);
          ```
       b. Calcular `thresholdPercentage` correctamente:
          ```ts
          const thresholdPercentage = max > 0 ? (item.threshold / max) * 100 : 15;
          ```
       c. Derivar `isCritical` con los valores corregidos.
       d. Mover el bloque del historial de movimientos DENTRO de la card del recurso, bajo la ProgressBar.
          - Mostrar hasta 3 movimientos (`item.movements.slice(0, 3)`).
          - Separador: `borderTopWidth: 1, borderTopColor: tc.border`.
          - Label: "ULTIMOS MOVIMIENTOS" en `tc.textMuted`.
          - Por movimiento: `+` o `-` en color `tc.success`/`tc.danger`, cantidad + unidad, fecha formateada.
       e. Texto de cantidad: `{item.currentAmount} / {max} {item.unit}` (usar `max` corregido).

    4. **Eliminar el bloque de historial externo (B-02):** Borrar el `allResources.map(...)` de las líneas ~160-184 que renderiza el historial fuera del FlatList. Ya no es necesario porque está dentro de renderItem.
  </action>
</task>

</tasks>

<verification>
- [ ] `Recurso` interface tiene `maxCapacity?: number`
- [ ] `darkTheme.colors.warningMuted` y `darkTheme.colors.warningBorder` existen y compilar sin error
- [ ] `lightTheme.colors.warningMuted` y `lightTheme.colors.warningBorder` existen
- [ ] En `resources.tsx`: no aparece ningún string `rgba(251` en el archivo
- [ ] En `resources.tsx`: no existe código fuera del `</FlatList>` que renderice historial
- [ ] En `resources.tsx`: `renderItem` calcula `max` usando `item.maxCapacity`
- [ ] En `resources.tsx`: `renderItem` muestra movimientos inline bajo la ProgressBar
- [ ] TypeScript compila sin errores nuevos (`npx tsc --noEmit`)
</verification>
