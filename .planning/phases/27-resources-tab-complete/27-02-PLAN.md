---
phase: 27-resources-tab-complete
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - app/(tabs)/resources.tsx
  - app/log-resource/index.tsx
autonomous: true
requirements: [LOGR-01, ERR-05]

must_haves:
  truths:
    - "Resource cards show a colored left border matching the resource category (oxigeno=cyan, agua=blue, comida=green, etc.)."
    - "Resource card header shows category symbol (O2, H2O, ALI, MED, EQP, OTR) beside the name."
    - "log-resource resource selector chips display currentAmount and unit beside the resource name."
    - "log-resource validates egreso amount does not exceed selectedResource.currentAmount before calling the API."
    - "log-resource uses selectedResource: Recurso | null state (not selectedResourceId: string)."
    - "All new code uses theme tokens — zero hardcoded hex or rgba strings."
  artifacts:
    - path: "app/(tabs)/resources.tsx"
      provides: "Category-colored resource cards with symbol indicators"
    - path: "app/log-resource/index.tsx"
      provides: "Egreso validation + amount display in resource selector + full Recurso object state"
  key_links:
    - from: "app/log-resource/index.tsx"
      to: "27-CONTEXT.md"
      via: "Bug B-06 + Mejoras M-03, D-09, D-10"
    - from: "app/(tabs)/resources.tsx"
      to: "src/constants/colors.ts"
      via: "categoryOxigeno, categoryAgua, etc. (líneas 66-73)"
---

<objective>
Elevar la calidad visual y la robustez funcional del tab de Recursos y el formulario de log-resource.

Purpose: Completar el ciclo recursos → registro de movimiento con validación correcta y contexto visual por categoría.
Output:
- Resource cards con borde e icono de categoría (colores semánticos existentes en colors.ts).
- log-resource con validación de egreso > currentAmount en el cliente.
- log-resource con estado `selectedResource: Recurso | null` para acceso al objeto completo.
- Chips de selector en log-resource muestran cantidad disponible.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-resources-tab-complete/27-CONTEXT.md
@app/(tabs)/resources.tsx
@app/log-resource/index.tsx
@src/constants/colors.ts
@src/types-dtos/recurso.dto.ts
@src/types-dtos/enums.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir CATEGORY_CONFIG y colores por categoría en resources.tsx</name>
  <files>app/(tabs)/resources.tsx</files>
  <action>
    1. Importar `colors` desde `@/constants/colors` al inicio del archivo.

    2. Definir `CATEGORY_CONFIG` como constante fuera del componente:
       ```ts
       import { colors } from '@/constants/colors';

       const CATEGORY_CONFIG: Record<string, { color: string; symbol: string }> = {
         oxigeno: { color: colors.categoryOxigeno, symbol: 'O2'  },
         agua:    { color: colors.categoryAgua,    symbol: 'H2O' },
         comida:  { color: colors.categoryComida,  symbol: 'ALI' },
         medico:  { color: colors.categoryMedico,  symbol: 'MED' },
         equipo:  { color: colors.categoryEquipo,  symbol: 'EQP' },
         otro:    { color: colors.categoryOtro,    symbol: 'OTR' },
       };
       ```

    3. En `renderItem`, resolver `catConfig`:
       ```ts
       const catConfig = CATEGORY_CONFIG[item.category] ?? CATEGORY_CONFIG['otro'];
       ```

    4. En el `View` de la card del recurso (el contenedor raíz de renderItem), añadir:
       ```ts
       borderLeftWidth: 3,
       borderLeftColor: catConfig.color,
       ```
       (Reemplaza el borde izquierdo plano actual si existe, o se añade al estilo existente.)

    5. En la fila del header de la card, añadir el símbolo de categoría antes del nombre:
       ```tsx
       <Text style={{ color: catConfig.color, fontFamily: 'monospace', fontSize: 9,
                      letterSpacing: 1, marginRight: 6 }}>
         {catConfig.symbol}
       </Text>
       ```
  </action>
</task>

<task type="auto">
  <name>Task 2: Cambiar selectedResourceId → selectedResource en log-resource</name>
  <files>app/log-resource/index.tsx</files>
  <action>
    1. Reemplazar el estado:
       ```ts
       // ANTES:
       const [selectedResourceId, setSelectedResourceId] = useState<string>('');
       // DESPUES:
       const [selectedResource, setSelectedResource] = useState<Recurso | null>(null);
       ```

    2. Actualizar `handleRegister`:
       - Condición de validación: `if (!selectedResource)` en lugar de `if (!selectedResourceId)`.
       - En `mutateAsync`: `recursoId: selectedResource.id`.
       - Deshabilitar submit: `(!selectedResource || !amount || isSubmitting)`.

    3. Actualizar `onPress` en los chips de recurso:
       ```tsx
       onPress={() => { setSelectedResource(resource); setValidationError(null); }}
       ```

    4. Actualizar la condición de selección activa en el chip:
       ```tsx
       selectedResource?.id === resource.id && { backgroundColor: tc.primary, borderColor: tc.primary }
       ```
       (en el estilo del chip)

    5. Actualizar el color del texto del chip seleccionado:
       ```tsx
       selectedResource?.id === resource.id && { color: tc.background }
       ```
  </action>
</task>

<task type="auto">
  <name>Task 3: Mostrar currentAmount en chips de selector de recurso</name>
  <files>app/log-resource/index.tsx</files>
  <action>
    - En el texto del chip de recurso, cambiar de solo `{resource.name.toUpperCase()}` a:
      ```tsx
      <Text style={[styles.chipText, { color: tc.textSecondary },
                   selectedResource?.id === resource.id && { color: tc.background }]}>
        {resource.name.toUpperCase()}
        {resource.currentAmount != null ? `  ${resource.currentAmount} ${resource.unit}` : ''}
      </Text>
      ```
    - Los dos espacios antes de la cantidad actúan como separador visual ligero.
    - `resource.unit` ya existe en el tipo `Recurso`.
  </action>
</task>

<task type="auto">
  <name>Task 4: Validación de egreso mayor a currentAmount</name>
  <files>app/log-resource/index.tsx</files>
  <action>
    - En `handleRegister`, después de la validación de `parsedAmount > 0` y antes de `mutateAsync`, añadir:
      ```ts
      if (movementType === 'egreso' && selectedResource && parsedAmount > selectedResource.currentAmount) {
        setValidationError(
          `Solo hay ${selectedResource.currentAmount} ${selectedResource.unit} disponibles de ${selectedResource.name}`
        );
        return;
      }
      ```
    - Este bloque usa el objeto `selectedResource` completo (disponible gracias a Task 2).
    - El error se mostrará en el `validationError` inline HUD que ya existe en el JSX del formulario.
  </action>
</task>

</tasks>

<verification>
- [ ] `resources.tsx` importa `colors` desde `@/constants/colors`
- [ ] `CATEGORY_CONFIG` definido con las 6 categorías del enum `ResourceCategory`
- [ ] Cards de recurso tienen `borderLeftWidth: 3` con color de categoría
- [ ] Cards de recurso muestran símbolo (O2, H2O, etc.) junto al nombre
- [ ] `log-resource/index.tsx` usa `selectedResource: Recurso | null` (no string)
- [ ] Chips de recurso muestran `NOMBRE  23.5 L` (nombre + cantidad + unidad)
- [ ] En egreso: intentar cantidad > currentAmount muestra mensaje en el validationError HUD
- [ ] No hay strings `rgba(` ni hex `#` hardcodeados en los archivos modificados
- [ ] TypeScript compila sin errores (`npx tsc --noEmit`)
- [ ] La app puede cargar el tab de Recursos sin crashes
- [ ] El formulario de log-resource permite registrar un ingreso y un egreso válidos
</verification>
