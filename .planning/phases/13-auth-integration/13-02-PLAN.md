---
phase: 13-auth-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/services/api.ts
  - app/_layout.tsx
  - app/(auth)/login.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - API-02
  - API-03
  - API-05
must_haves:
  truths:
    - "User can log in and is routed to the home tab without back navigation"
    - "Protected routes redirect unauthenticated users to login"
    - "401 responses trigger refresh and retry without user action"
  artifacts:
    - path: "src/services/api.ts"
      provides: "Axios interceptors with refresh queue"
    - path: "app/_layout.tsx"
      provides: "Stack.Protected guard for (app) group"
    - path: "app/(auth)/login.tsx"
      provides: "Login screen calling login mutation"
  key_links:
    - from: "src/services/api.ts"
      to: "/auth/refresh"
      via: "axios.post refresh flow"
      pattern: "auth/refresh"
    - from: "app/_layout.tsx"
      to: "(app) group"
      via: "Stack.Protected"
      pattern: "Stack\.Protected"
    - from: "app/(auth)/login.tsx"
      to: "router.replace"
      via: "login success navigation"
      pattern: "router\.replace\('/\(app\)/\(tabs\)/home'\)"
---

<objective>
Wire login-only flow: login screen uses auth mutation, API client refresh interceptor handles 401s, and protected routes guard the app group.

Purpose: Deliver secure login and session continuity without register flow (deferred).
Output: Updated API interceptors, Stack.Protected routing, and login navigation.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-auth-integration/13-CONTEXT.md
@.planning/phases/13-auth-integration/13-RESEARCH.md
@src/services/api.ts
@app/_layout.tsx
@app/(auth)/login.tsx
</context>

<interfaces>

From src/stores/auth.store.ts (expected shape after 13-01):
```typescript
accessToken: string | null;
refreshToken: string | null;
user: AuthUser | null;
isAuthenticated: boolean;
isLoading: boolean;
setAuth(accessToken: string, refreshToken: string, user: AuthUser): void;
clearAuth(): void;
```

From src/services/auth.service.ts:
```typescript
login(data: LoginDTO): Promise<LoginResponse>;
refresh(refreshToken: string): Promise<{ accessToken: string; refreshToken: string }>;
logout(): Promise<void>;
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add refresh-token response interceptor with queue</name>
  <files>src/services/api.ts</files>
  <read_first>
    - src/services/api.ts
    - src/stores/auth.store.ts
    - src/services/auth.service.ts
    - .planning/phases/13-auth-integration/13-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: 401 triggers single refresh call and retries original request
    - Test 2: parallel 401s wait on refresh queue
    - Test 3: refresh failure clears auth and rejects request
  </behavior>
  <action>
    Update src/services/api.ts to match decisions D-01 to D-03:
    - Keep API_BASE_URL using EXPO_PUBLIC_API_URL fallback (API-05)
    - Request interceptor reads accessToken from useAuthStore.getState().accessToken (API-02)
    - Response interceptor:
      - If no response: reject with message 'Network error. Please check your connection.'
      - If status 401 and originalRequest not retried:
        - If isRefreshing true, push resolver to failedQueue and retry with new token
        - Else set isRefreshing, call authService.refresh(refreshToken)
        - On success: setAuth(new accessToken, new refreshToken, current user) and retry
        - On failure (401/403): clearAuth() and return rejected error
      - Handle 403/400/500 with user-friendly messages (API-03)
    - Keep response unwrapping if currently used (note: api.ts currently returns full response; align with existing service usage)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - "src/services/api.ts contains isRefreshing and failedQueue logic"
    - "src/services/api.ts calls authService.refresh in 401 handler"
    - "src/services/api.ts updates Authorization header before retry"
    - "src/services/api.ts uses EXPO_PUBLIC_API_URL fallback"
  </acceptance_criteria>
  <done>
    API client refreshes tokens on 401 and retries requests without user interaction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace AuthGuard with Stack.Protected for (app) group</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - app/_layout.tsx
    - app/(auth)/_layout.tsx
    - .planning/PROJECT.md
    - .planning/phases/13-auth-integration/13-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Unauthenticated user is redirected to /(auth)/login
    - Test 2: Authenticated user can access /(app) routes
  </behavior>
  <action>
    Update app/_layout.tsx to use Expo Router Stack.Protected per D-09 to D-11:
    - Remove custom AuthGuard component
    - Keep QueryClientProvider wrapping Stack (existing queryClient)
    - Use useAuthStore to read isAuthenticated and isLoading
    - While isLoading, render centered ActivityIndicator using theme colors (tc.primary)
    - Stack structure:
      - <Stack.Screen name="(auth)" options={{ headerShown: false }} />
      - <Stack.Protected group="(app)" guard={() => isAuthenticated}>
          <Stack.Screen name="(app)" options={{ headerShown: false }} />
        </Stack.Protected>
    - Do NOT introduce register screen
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - "app/_layout.tsx contains <Stack.Protected group=\"(app)\""
    - "app/_layout.tsx does not contain AuthGuard component"
    - "app/_layout.tsx renders ActivityIndicator when isLoading is true"
  </acceptance_criteria>
  <done>
    Protected routes use Stack.Protected with auth guard and loading state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire login screen to auth mutation and router.replace</name>
  <files>app/(auth)/login.tsx</files>
  <read_first>
    - app/(auth)/login.tsx
    - src/hooks/useAuth.ts
    - src/services/auth.service.ts
    - .planning/phases/13-auth-integration/13-CONTEXT.md
    - .planning/PROJECT.md
  </read_first>
  <behavior>
    - Test 1: Successful login calls router.replace('/(app)/(tabs)/home')
    - Test 2: Login uses email/password fields mapped to LoginDTO
  </behavior>
  <action>
    Update app/(auth)/login.tsx to use login mutation:
    - Keep existing HUD UI/animations; do not remove visual effects unless needed for functionality
    - Replace useAuthStore login() usage with useLogin() mutation from src/hooks/useAuth
    - Map inputs to LoginDTO: email and password (rename agentId state to email but keep visual label if desired)
    - On success: call useAuthStore.getState().setAuth(accessToken, refreshToken, user) and router.replace('/(app)/(tabs)/home') (per D-12, D-14)
    - On error: show existing error state (keep minimal UI change)
    - Do NOT add register link or register screen
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - "app/(auth)/login.tsx uses useLogin mutation"
    - "app/(auth)/login.tsx calls router.replace('/(app)/(tabs)/home') on success"
    - "app/(auth)/login.tsx passes email/password to login"
  </acceptance_criteria>
  <done>
    Login screen authenticates via API and navigates to protected home route.
  </done>
</task>

</tasks>

<verification>
Run `npm run lint` to confirm lint + TypeScript checks pass.
</verification>

<success_criteria>
- Login routes to /(app)/(tabs)/home with router.replace
- Protected routes are enforced via Stack.Protected
- 401 refresh flow retries requests and clears auth on failure
</success_criteria>

<output>
After completion, create `.planning/phases/13-auth-integration/13-02-SUMMARY.md`
</output>
