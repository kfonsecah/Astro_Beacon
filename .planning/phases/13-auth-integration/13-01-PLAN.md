---
phase: 13-auth-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types-dtos/sesion.dto.ts
  - src/services/auth.service.ts
  - src/stores/auth.store.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - API-06
must_haves:
  truths:
    - "Auth tokens are stored in expo-secure-store with separate access/refresh keys"
    - "Auth state rehydrates on app start and sets isAuthenticated accordingly"
  artifacts:
    - path: "src/types-dtos/sesion.dto.ts"
      provides: "Auth DTOs aligned to backend response envelope"
    - path: "src/services/auth.service.ts"
      provides: "Auth API calls using axios instance"
    - path: "src/stores/auth.store.ts"
      provides: "Zustand auth store with SecureStore persistence"
  key_links:
    - from: "src/stores/auth.store.ts"
      to: "expo-secure-store"
      via: "SecureStore.getItemAsync/setItemAsync"
      pattern: "SecureStore\\.(getItemAsync|setItemAsync|deleteItemAsync)"
    - from: "src/stores/auth.store.ts"
      to: "zustand/middleware"
      via: "persist + createJSONStorage"
      pattern: "persist\\("
---

<objective>
Align auth DTOs and service shapes with backend token responses and persist auth state securely in SecureStore (login-only scope).

Purpose: Ensure tokens are stored securely and survive app restarts without introducing register flow (per D-07).
Output: Updated auth DTOs, auth service returning correct token fields, and persisted auth store with SecureStore keys.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-auth-integration/13-CONTEXT.md
@.planning/phases/13-auth-integration/13-RESEARCH.md
@src/types-dtos/sesion.dto.ts
@src/services/auth.service.ts
@src/stores/auth.store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align auth DTOs to backend token fields (login-only)</name>
  <files>src/types-dtos/sesion.dto.ts</files>
  <read_first>
    - src/types-dtos/sesion.dto.ts
    - .planning/phases/13-auth-integration/13-CONTEXT.md
    - .planning/phases/13-auth-integration/13-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: LoginResponse includes accessToken + refreshToken fields (not token)
    - Test 2: LoginResponse user object matches backend user fields used in app (id, nombre, email, rol)
  </behavior>
  <action>
    Update LoginResponse in src/types-dtos/sesion.dto.ts to match backend envelope from Phase 09:
    - Replace token field with accessToken: string
    - Ensure refreshToken: string exists
    - Keep expiraEn: Date (if backend includes), else leave as optional if uncertain
    - Ensure astronauta object fields remain: id, nombre, email, rol
    Do NOT add RegisterDTO usage in this plan (register deferred per D-07).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - "src/types-dtos/sesion.dto.ts contains interface LoginResponse with accessToken and refreshToken"
    - "src/types-dtos/sesion.dto.ts does not contain LoginResponse.token"
  </acceptance_criteria>
  <done>
    LoginResponse matches backend auth payload with accessToken/refreshToken and astronauta fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update auth service shapes for login/refresh/logout</name>
  <files>src/services/auth.service.ts</files>
  <read_first>
    - src/services/auth.service.ts
    - src/types-dtos/sesion.dto.ts
    - src/services/api.ts
    - .planning/phases/13-auth-integration/13-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: login() returns LoginResponse with accessToken/refreshToken
    - Test 2: refresh() returns accessToken + refreshToken (backend returns both)
  </behavior>
  <action>
    In src/services/auth.service.ts:
    - Keep login() calling POST /auth/login (baseURL already includes /api/v1)
    - Ensure response typing uses { success: boolean; data: LoginResponse } and returns response.data.data
    - Update refresh() to return both accessToken and refreshToken (use response.data.data fields), not just accessToken
    - Keep logout() POST /auth/logout
    - Leave register() function present but unused; do not wire to UI in this phase
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - "src/services/auth.service.ts refresh() returns accessToken and refreshToken"
    - "src/services/auth.service.ts login() returns response.data.data"
    - "src/services/auth.service.ts keeps /auth/login and /auth/refresh endpoints"
  </acceptance_criteria>
  <done>
    Auth service returns backend token fields used by store/interceptors without register wiring.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist auth state in SecureStore with access/refresh keys</name>
  <files>src/stores/auth.store.ts</files>
  <read_first>
    - src/stores/auth.store.ts
    - src/services/auth.service.ts
    - .planning/phases/13-auth-integration/13-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Store persists accessToken, refreshToken, user under SecureStore keys
    - Test 2: Rehydration sets isAuthenticated true when accessToken exists
  </behavior>
  <action>
    Update src/stores/auth.store.ts to follow decisions D-04, D-05, D-15, D-16, D-17:
    - Replace mock login/logout/checkAuth with real state actions (no API call in store)
    - State shape: { accessToken, refreshToken, user, isAuthenticated, isLoading }
    - Use zustand persist with createJSONStorage + SecureStore adapter
    - SecureStore keys: 'access_token', 'refresh_token', 'user_data'
    - Migrate old 'auth_token' key: on init, if auth_token exists, move value to access_token and delete auth_token
    - Provide actions: setAuth(accessToken, refreshToken, user), clearAuth(), setLoading()
    - On rehydrate: set isAuthenticated true if accessToken exists, set isLoading false
    - Do NOT include register action (deferred)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - "src/stores/auth.store.ts uses persist( and createJSONStorage"
    - "src/stores/auth.store.ts contains SecureStore keys 'access_token' and 'refresh_token'"
    - "src/stores/auth.store.ts deletes legacy key 'auth_token' during migration"
    - "src/stores/auth.store.ts exports useAuthStore with accessToken/refreshToken/user"
  </acceptance_criteria>
  <done>
    Auth state persists securely and rehydrates with correct tokens without register flow.
  </done>
</task>

</tasks>

<verification>
Run `npm run lint` after changes to confirm TypeScript + lint passes.
</verification>

<success_criteria>
- SecureStore persistence uses access_token/refresh_token/user_data keys
- Auth store rehydrates and sets isAuthenticated based on accessToken
- Auth service returns token fields expected by interceptor and UI
</success_criteria>

<output>
After completion, create `.planning/phases/13-auth-integration/13-01-SUMMARY.md`
</output>
