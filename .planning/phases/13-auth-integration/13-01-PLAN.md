---
phase: 13-auth-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/auth.store.ts
  - app/(auth)/register.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-08, API-06]
user_setup: []
must_haves:
  truths:
    - "User can log in with email/password and is redirected to home screen with no back button to login"
    - "User can register a new account with email/password and receive confirmation"
    - "JWT and refresh tokens are stored securely in expo-secure-store"
    - "After login/register, navigation stack is replaced to prevent back to login"
  artifacts:
    - path: "src/stores/auth.store.ts"
      provides: "Auth state management with Zustand persist middleware"
      contains: "persist("
    - path: "app/(auth)/register.tsx"
      provides: "Register screen with email/password inputs"
      exists: true
  key_links:
    - from: "src/stores/auth.store.ts"
      to: "expo-secure-store"
      via: "persist middleware with secureStorage adapter"
      pattern: "SecureStore\\.setItemAsync"
    - from: "src/stores/auth.store.ts"
      to: "api.ts"
      via: "login() calls api.post('/auth/login')"
      pattern: "api\\.post\\('/auth/login'"
---

<objective>
Update auth store with real API integration and create the missing register screen.

Purpose: Replace mock implementation with real API calls, add Zustand persist middleware for SecureStore, and create register screen for AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-08, API-06.
Output: Working auth store with persistent storage + register screen.
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Astro_Beacon/.planning/phases/13-auth-integration/13-CONTEXT.md
@/home/brian/4_anno/Moviles/Astro_Beacon/.planning/phases/09-autenticaci-n/09-CONTEXT.md
</execution_context>

<context>
@/home/brian/4_anno/Moviles/Astro_Beacon/src/stores/auth.store.ts
@/home/brian/4_anno/Moviles/Astro_Beacon/src/services/api.ts
@/home/brian/4_anno/Moviles/Astro_Beacon/app/(auth)/login.tsx
@/home/brian/4_anno/Moviles/Astro_Beacon/.planning/ROADMAP.md
@/home/brian/4_anno/Moviles/Astro_Beacon/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth.store.ts with persist middleware and real API calls</name>
  <files>src/stores/auth.store.ts</files>
  <read_first>
    - src/stores/auth.store.ts (current mock implementation)
    - src/services/api.ts (axios instance to use)
    - .planning/phases/13-auth-integration/13-CONTEXT.md (decisions D-04 to D-08, D-15 to D-17)
  </read_first>
  <action>
    Replace the entire contents of `src/stores/auth.store.ts` with the following implementation. This implements decisions D-04, D-05, D-06, D-07, D-08, D-15, D-16, D-17:

    1. Add imports for `persist` and `createJSONStorage` from `zustand/middleware`
    2. Create a `secureStorage` adapter object with `getItem`, `setItem`, `removeItem` methods using `expo-secure-store`
    3. Update the `AuthState` interface to include: `accessToken`, `refreshToken` (both string | null), update `login` signature to accept `(email: string, password: string)`, add `register: (email: string, password: string) => Promise<void>`
    4. Remove the old `agentId` parameter from login - use `email` instead (per CONTEXT.md and backend API)
    5. Implement `login()`:
       - Call `api.post('/auth/login', { email, password })` (not '/api/v1/auth/login' - api.ts already has baseURL)
       - Extract `accessToken`, `refreshToken`, `user` from `response.data.data` (backend returns { success, data: { accessToken, refreshToken, user } })
       - Return the response (store will be updated via persist middleware)
    6. Implement `register()`:
       - Call `api.post('/auth/register', { email, password })`
       - Extract same fields as login
       - Return the response
    7. Implement `logout()`:
       - Call `api.post('/auth/logout')` to revoke refresh token on backend
       - Persist middleware will clear state
    8. Implement `checkAuth()`:
       - Persist middleware auto-rehydrates, just set `isLoading: false`
    9. Wrap create() with `persist()` middleware:
       - name: 'auth-storage'
       - storage: `createJSONStorage(() => secureStorage)`
       - Only persist: `accessToken`, `refreshToken`, `user` (not isLoading, isAuthenticated)
    10. Change storage keys from 'auth_token'/'user_data' to 'access_token'/'refresh_token'/'user_data' (D-15, D-17)
    11. Export `useAuth` as alias (keep existing export)

    The api import should be: `import { api } from '@/services/api';` (not axios directly)

    Important: The persist middleware will automatically sync to SecureStore. The `login` and `register` functions should return the api.post promise so the caller can use .then() or await.
  </action>
  <verify>
    <automated>grep -q "persist(" src/stores/auth.store.ts && grep -q "access_token" src/stores/auth.store.ts && grep -q "refresh_token" src/stores/auth.store.ts && grep -q "api.post('/auth/login'" src/stores/auth.store.ts && grep -q "api.post('/auth/register'" src/stores/auth.store.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - auth.store.ts uses Zustand persist middleware with SecureStore adapter
    - login() calls POST /auth/login with email/password
    - register() calls POST /auth/register with email/password
    - logout() calls POST /auth/logout
    - Storage keys are 'access_token', 'refresh_token', 'user_data' (not old 'auth_token')
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create register screen at app/(auth)/register.tsx</name>
  <files>app/(auth)/register.tsx</files>
  <read_first>
    - app/(auth)/login.tsx (reference for styling and animation patterns)
    - app/(auth)/_layout.tsx (layout wrapper)
    - .planning/phases/13-auth-integration/13-CONTEXT.md (decisions D-12, D-13, D-14)
    - hooks/use-theme.ts (for theme access)
  </read_first>
  <action>
    Create `app/(auth)/register.tsx` with the following implementation. This implements AUTH-02 and follows the existing login screen patterns (D-12, D-13, D-14):

    1. Import required modules:
       - `useAuth` from '@/stores/auth.store'
       - `useTheme` from '@/hooks/use-theme'
       - `useRouter` from 'expo-router'
       - `useState`, `useCallback` from 'react'
       - `KeyboardAvoidingView`, `Platform`, `Pressable`, `SafeAreaView`, `ScrollView`, `Text`, `TextInput`, `View` from 'react-native'
       - `Animated` components: `FadeIn`, `SlideInDown`, `useAnimatedStyle`, `useSharedValue`, `withTiming` from 'react-native-reanimated'
       - Reuse `StarField`, `Scanlines`, `ShootingStar`, `BlinkingCursor`, `PulsingDot` components from login.tsx (or recreate minimal versions)

    2. Component state:
       - `email` (string), `password` (string), `confirmPassword` (string)
       - `showPassword` (boolean), `showConfirmPassword` (boolean)
       - `isRegistering` (boolean), `focusedField` (string | null)

    3. Handle registration:
       - `handleRegister` callback: validates email/password not empty, passwords match
       - Calls `useAuth.getState().register(email, password)` (use getState to avoid re-renders)
       - On success: `router.replace('/(app)/(tabs)/home')` (D-12, D-14 - no back to login)
       - On error: sets `isRegistering` to false, shows alert with error message

    4. UI Structure (matching login.tsx style):
       - SafeAreaView with background color from theme
       - StarField and Scanlines components (same as login)
       - KeyboardAvoidingView with Platform.OS behavior
       - ScrollView with content container
       - Animated header text: "ASTRO" (same as login)
       - Subtitle: "REGISTRO DE AGENTE" (agent registration)
       - Version text: "v2.7.1 — NUEVO AGENTE"

    5. Form fields:
       - Email input (label: "CORREO ELECTRÓNICO")
         - autoCapitalize="none", keyboardType="email-address"
         - Stores as `email` state
       - Password input (label: "CLAVE DE ACCESO")
         - secureTextEntry toggle via showPassword
         - Stores as `password` state
       - Confirm Password input (label: "CONFIRMAR CLAVE")
         - secureTextEntry toggle via showConfirmPassword
         - Stores as `confirmPassword` state
         - Validate match before submit

    6. Register button:
       - Text: "[ REGISTRAR ]" when not registering
       - Text: "REGISTRANDO..." when registering
       - Disabled if email/password empty or passwords don't match
       - Uses Animated.View with pressIn/pressOut scaling (same as login)
       - onPress calls handleRegister

    7. Connection status indicator (same as login)

    8. Navigation hint: "¿Ya tienes cuenta? INICIAR SESIÓN" - tappable, navigates to /(auth)/login

    Note: Use `router.replace('/(app)/(tabs)/home')` after successful registration (D-12, D-14). The (app) group will be created in Plan 02.
  </action>
  <verify>
    <automated>test -f app/"(auth)"/register.tsx && grep -q "router.replace('/(app)/(tabs)/home')" app/"(auth)"/register.tsx && grep -q "register" app/"(auth)"/register.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Register screen exists at app/(auth)/register.tsx
    - Has email, password, confirm password inputs
    - Calls register() from auth store on submit
    - Navigates to /(app)/(tabs)/home after success (router.replace, not navigate)
    - Matches login screen visual style (stars, scanlines, monospace font)
    - Validates password confirmation before submit
  </done>
</task>

</tasks>

<verification>
- auth.store.ts has persist middleware with SecureStore
- auth.store.ts login() calls real /auth/login endpoint
- auth.store.ts register() calls real /auth/register endpoint
- auth.store.ts uses 'access_token', 'refresh_token', 'user_data' keys
- Register screen exists with email/password/confirm inputs
- Register screen navigates to /(app)/(tabs)/home on success
</verification>

<success_criteria>
- [ ] AUTH-01: login() calls POST /api/v1/auth/login
- [ ] AUTH-02: register screen exists and calls POST /api/v1/auth/register
- [ ] AUTH-03, AUTH-04: Tokens stored in expo-secure-store via persist middleware
- [ ] AUTH-08: Navigation uses router.replace() after login/register
- [ ] API-06: Zustand persist middleware syncs with expo-secure-store
</success_criteria>

<output>
After completion, create `.planning/phases/13-auth-integration/13-01-SUMMARY.md`
</output>
