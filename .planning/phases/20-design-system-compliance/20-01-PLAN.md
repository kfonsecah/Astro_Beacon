---
phase: 20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/constants/colors.ts, app/(tabs)/map.tsx, app/trips.tsx]
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "No hardcoded hex values remain in map.tsx or trips.tsx"
    - "New color constants exist in colors.ts for trip status and supply categories"
    - "Application UI remains visually identical to previous state"
  artifacts:
    - path: "src/constants/colors.ts"
      provides: "New color constants for Trip Status and Supply Categories"
    - path: "app/(tabs)/map.tsx"
      provides: "Compliant map screen using colors constants"
    - path: "app/trips.tsx"
      provides: "Compliant trips screen using colors constants"
  key_links:
    - from: "app/(tabs)/map.tsx"
      to: "src/constants/colors.ts"
      via: "import { colors }"
    - from: "app/trips.tsx"
      to: "src/constants/colors.ts"
      via: "import { colors }"
---

<objective>
Eliminate hardcoded hex color values in map.tsx and trips.tsx by extending the centralized colors.ts constant file. This ensures compliance with the Phase 07 Design System invariant.

Purpose: Maintain consistency across the application and enable single-point color updates.
Output: Updated colors.ts with domain-specific constants and refactored screens.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-design-system-compliance/20-CONTEXT.md
@.planning/phases/20-design-system-compliance/20-RESEARCH.md
@src/constants/colors.ts
@app/(tabs)/map.tsx
@app/trips.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend colors.ts with domain-specific constants</name>
  <files>src/constants/colors.ts</files>
  <action>
    Add the following constants to `src/constants/colors.ts` as specified in RESEARCH.md:

    1. **Trip status** (align with existing semantic palette):
       - `tripPlanificado: '#F59E0B'`
       - `tripActivo: '#22C55E'`
       - `tripCompletado: '#3B82F6'`
       - `tripAbortado: '#EF4444'`

    2. **Supply categories** (HUD-compatible palette):
       - `categoryOxigeno: '#22D3EE'`
       - `categoryAgua: '#3B82F6'`
       - `categoryComida: '#84CC16'`
       - `categoryMedico: '#EC4899'`
       - `categoryEquipo: '#FB923C'`
       - `categoryOtro: '#6B7280'`

    3. **UI Support**:
       - `shadowBlack: '#000000'`

    Ensure these are added inside the `colors` export object, maintaining the existing commented section pattern.
  </action>
  <verify>
    <automated>grep -E "tripPlanificado|categoryOxigeno|shadowBlack" src/constants/colors.ts</automated>
  </verify>
  <done>New constants are exported in colors.ts</done>
</task>

<task type="auto">
  <name>Task 2: Refactor map.tsx to use colors constants</name>
  <files>app/(tabs)/map.tsx</files>
  <action>
    1. Add `import { colors } from '@/constants/colors';` at the top of `app/(tabs)/map.tsx`.
    2. Replace hex values in `statusColorMap` (lines 16-19):
       - `pendiente: colors.supplyPendiente`
       - `entregado: colors.supplyEntregado`
       - `recogido: colors.supplyRecogido`
       - `expirado: colors.supplyExpirado`
    3. Replace hex values in `categoryConfig` (lines 30-35):
       - `oxigeno: { ..., color: colors.categoryOxigeno }`
       - `agua: { ..., color: colors.categoryAgua }`
       - `comida: { ..., color: colors.categoryComida }`
       - `medico: { ..., color: colors.categoryMedico }`
       - `equipo: { ..., color: colors.categoryEquipo }`
       - `otro: { ..., color: colors.categoryOtro }`
    4. Search for any remaining `shadowColor: '#000'` or similar and replace with `colors.shadowBlack` or `colors.overlayDark` as per discretion (if found).
  </action>
  <verify>
    <automated>grep "#[0-9a-fA-F]" "app/(tabs)/map.tsx" | grep -v "tc\."</automated>
  </verify>
  <done>map.tsx has zero hardcoded hex values and uses colors constants</done>
</task>

<task type="auto">
  <name>Task 3: Refactor trips.tsx to use colors constants</name>
  <files>app/trips.tsx</files>
  <action>
    1. Add `import { colors } from '@/constants/colors';` at the top of `app/trips.tsx`.
    2. Replace hex values in `statusColorMap` (lines 11-14):
       - `planificado: colors.tripPlanificado`
       - `activo: colors.tripActivo`
       - `completado: colors.tripCompletado`
       - `abortado: colors.tripAbortado`
  </action>
  <verify>
    <automated>grep "#[0-9a-fA-F]" "app/trips.tsx" | grep -v "tc\."</automated>
  </verify>
  <done>trips.tsx has zero hardcoded hex values and uses colors constants</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme Constants | Internal boundary; values in colors.ts are trusted and used for UI styling only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | colors.ts | accept | Publicly accessible colors are not sensitive information. |
| T-20-02 | Tampering | colors.ts | mitigate | Ensure constants are exported as read-only objects (already standard pattern). |
</threat_model>

<verification>
Run the following command to verify compliance across both modified screens:
`grep -E "#[0-9a-fA-F]{3,6}" "app/(tabs)/map.tsx" "app/trips.tsx"`
The output should be empty (excluding any theme color references if they use hex, though project patterns use `tc.*`).
</verification>

<success_criteria>
1. `src/constants/colors.ts` contains the new constants.
2. `app/(tabs)/map.tsx` and `app/trips.tsx` import and use `colors`.
3. No hardcoded hex values starting with `#` remain in `map.tsx` and `trips.tsx`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-design-system-compliance/20-01-SUMMARY.md`
</output>
