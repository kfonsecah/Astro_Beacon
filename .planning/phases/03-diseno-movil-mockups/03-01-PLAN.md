---
phase: 03-diseno-movil-mockups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/login.tsx
  - app/(tabs)/dashboard.tsx
  - app/(tabs)/bestiary.tsx
  - app/(tabs)/resources.tsx
  - app/(tabs)/map.tsx
  - app/(tabs)/logbook.tsx
  - app/_layout.tsx
  - app/(auth)/_layout.tsx
  - app/(tabs)/_layout.tsx
autonomous: true
requirements:
  - "Diseño móvil - Rúbrica Base Inicial"
must_haves:
  truths:
    - "6 screens implemented with HUD aesthetic (navy background, cyan accents, monospace data)"
    - "Route groups configured: (auth)/ and (tabs)/ with expo-router"
    - "Bottom tabs with 5 tabs: Dashboard, Bitácora, Recursos, Mapa, Registros"
    - "Mock data matches entity types from Phase 2 (src/types-dtos/)"
  artifacts:
    - path: "app/(auth)/login.tsx"
      provides: "Login screen placeholder with HUD styling"
    - path: "app/(tabs)/dashboard.tsx"
      provides: "Dashboard HUD with resource bars and mission progress"
    - path: "app/(tabs)/bestiary.tsx"
      provides: "Species catalog with card grid layout"
    - path: "app/(tabs)/resources.tsx"
      provides: "Resource management screen"
    - path: "app/(tabs)/map.tsx"
      provides: "Map placeholder with supply drop markers"
    - path: "app/(tabs)/logbook.tsx"
      provides: "Discovery journal with entries list"
---

<objective>
Create 6 screen mockups as React Native components with HUD/space aesthetic from the reference project. Screens use mock data and serve as visual prototypes for the Base Inicial defense.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Set up route groups and navigation structure</name>
  <files>app/_layout.tsx, app/(auth)/_layout.tsx, app/(tabs)/_layout.tsx</files>
  <action>
    1. Create `app/(auth)/_layout.tsx` — Stack navigator for auth screens (no header)
    2. Create `app/(tabs)/_layout.tsx` — Bottom tabs with 5 tabs:
       - dashboard: "Panel" (Ionicons: planet-outline)
       - bestiary: "Bitácora" (Ionicons: bug-outline)
       - resources: "Recursos" (Ionicons: cube-outline)
       - map: "Mapa" (Ionicons: map-outline)
       - logbook: "Registros" (Ionicons: journal-outline)
       Tab styling: activeTintColor #6EE7B7, inactive #6B7280, backgroundColor #0B1120
    3. Update `app/_layout.tsx` — Root Stack with (auth) and (tabs) groups
    4. Create `app/(auth)/login.tsx` — Login screen with:
       - HUD-styled input for agent ID
       - Login button with cyan border, navy background
       - "Houston, we need you" subtitle
       - Monospace typography for labels
  </action>
  <verify>app/(auth)/_layout.tsx, app/(tabs)/_layout.tsx, app/(auth)/login.tsx exist with correct structure</verify>
  <done>Route groups configured, tab layout with 5 tabs, login screen with HUD styling</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard screen (main HUD)</name>
  <files>app/(tabs)/dashboard.tsx</files>
  <action>
    Create `app/(tabs)/dashboard.tsx` with:
    - Header: "PANEL DE CONTROL" in monospace, cyan color, letter-spacing
    - Resource bars section (mock data): Oxígeno 87%, Agua 62%, Comida 45%
      - Each bar: label, segmented progress bar, percentage
      - Critical threshold styling (red when < 15%)
    - Mission progress section: "Día 47 en planeta desconocido"
    - Alert banner: placeholder for resource warnings
    - Quick actions: FAB or buttons for "Iniciar Expedición", "Tomar Foto"
    - Dark navy background (#0B1120), cyan accents (#6EE7B7)
    - Use mock data matching Recurso type from Phase 2
  </action>
  <verify>Dashboard screen shows resource bars, mission progress, HUD aesthetic</verify>
  <done>Dashboard screen complete with HUD styling and mock data</done>
</task>

<task type="auto">
  <name>Task 3: Create Bestiary screen (species catalog)</name>
  <files>app/(tabs)/bestiary.tsx</files>
  <action>
    Create `app/(tabs)/bestiary.tsx` with:
    - Header: "BITÁCORA DE ESPECIES" in monospace
    - Grid/list of species cards (mock data, 4-6 entries):
      - Each card: thumbnail placeholder, species name, classification badge, danger level indicator
      - Classification badges: colored pills (animal=green, plant=brown, resource=blue, unknown=gray)
      - Danger level: colored dot (friendly=green, dangerous=red, lethal=purple)
    - Pull-to-refresh indicator (ScrollView with RefreshControl)
    - Search/filter bar placeholder
    - Empty state: "No se han descubierto especies aún" with icon
    - Navigate to /species/[id] on card press (placeholder)
  </action>
  <verify>Bestiary shows species cards with classification and danger badges</verify>
  <done>Bestiary screen complete with species grid and HUD styling</done>
</task>

<task type="auto">
  <name>Task 4: Create Resources screen</name>
  <files>app/(tabs)/resources.tsx</files>
  <action>
    Create `app/(tabs)/resources.tsx` with:
    - Header: "GESTIÓN DE RECURSOS"
    - Full resource list (mock data for all categories):
      - Oxígeno, Agua, Comida, Médico, Equipo
      - Each: name, current/max, segmented bar, unit
    - Resource movement history section (last 3-5 entries):
      - Date, type (ingreso/egreso), amount, reason
    - "Registrar Movimiento" button
    - Alert section for resources below threshold
  </action>
  <verify>Resources screen shows full inventory and movement history</verify>
  <done>Resources screen complete with inventory and history</done>
</task>

<task type="auto">
  <name>Task 5: Create Map screen</name>
  <files>app/(tabs)/map.tsx</files>
  <action>
    Create `app/(tabs)/map.tsx` with:
    - Header: "MAPA DE EXPLORACIÓN"
    - Map placeholder area with:
      - Grid pattern overlay (HUD aesthetic)
      - Mock supply drop markers (3-4 pins with distance labels)
      - Current location indicator (center)
      - "Nave" marker (base camp)
    - Supply drop list below map:
      - Each: status badge (pendiente/entregado/recogido), distance, contents preview
    - "Iniciar Expedición" button → navigates to /exploration/
    - Scan line animation placeholder (animated View)
  </action>
  <verify>Map screen shows placeholder with supply drop markers and list</verify>
  <done>Map screen complete with supply drops and expedition button</done>
</task>

<task type="auto">
  <name>Task 6: Create Logbook screen</name>
  <files>app/(tabs)/logbook.tsx</files>
  <action>
    Create `app/(tabs)/logbook.tsx` with:
    - Header: "REGISTROS DE MISIÓN"
    - Timeline/list of log entries (mock data, 4-6 entries):
      - Each: date, description preview, species icon, sync status indicator
      - Offline entries marked with "⏳ Pendiente de sync"
      - Synced entries marked with "✅ Sincronizado"
    - "Nueva Entrada" FAB (floating action button)
    - Pull-to-refresh
    - Filter by date/species placeholder
  </action>
  <verify>Logbook shows entries with sync status and FAB</verify>
  <done>Logbook screen complete with timeline and sync indicators</done>
</task>

</tasks>

<verification>
- All 6 screens exist in correct route group locations
- Tab layout has 5 tabs with correct icons and labels
- All screens use HUD color palette (#0B1120 bg, #6EE7B7 accent)
- Monospace typography for headers and data
- Mock data matches Phase 2 TypeScript types
- Navigation between screens works (tabs, stack pushes)
- Pull-to-refresh on list screens
- FAB on logbook screen
</verification>

<success_criteria>
- Screens are visually coherent with the HUD/space aesthetic from astro-beacon-reference/
- Navigation structure matches the route mapping from research
- Mock data demonstrates understanding of entity relationships from Phase 2
- Mobile metaphors are present (tabs, pull-to-refresh, FAB, stack navigation)
- Screens can be run in Expo Go for demonstration
</success_criteria>

<output>
After completion, create `.planning/phases/03-diseno-movil-mockups/03-01-SUMMARY.md`
</output>
