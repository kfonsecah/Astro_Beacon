---
phase: 04-design-system-y-estructura-visual
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Button.styles.ts
  - src/components/ui/Card.tsx
  - src/components/ui/Card.styles.ts
  - src/components/ui/Input.tsx
  - src/components/ui/Input.styles.ts
  - src/components/ui/Badge.tsx
  - src/components/ui/Badge.styles.ts
  - src/components/ui/ProgressBar.tsx
  - src/components/ui/ProgressBar.styles.ts
  - src/components/ui/EmptyState.tsx
  - src/components/ui/EmptyState.styles.ts
  - src/components/ui/index.ts
  - src/components/common/HudHeader.tsx
  - src/components/common/HudHeader.styles.ts
autonomous: true
requirements:
  - DS-05
must_haves:
  truths:
    - "Button renders with primary/secondary/ghost variants using theme colors"
    - "Card renders with HUD-style border and zero border-radius"
    - "Input renders with proper placeholder color and focus state from theme"
    - "Badge renders with color prop and outline/ghost variants"
    - "ProgressBar renders segmented bar matching dashboard's existing resource bar pattern"
    - "EmptyState renders when no data is available"
    - "HudHeader renders screen title with HUD styling"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Button with primary/secondary/ghost variants"
      exports: ["Button"]
    - path: "src/components/ui/Card.tsx"
      provides: "Card container with title support"
      exports: ["Card"]
    - path: "src/components/ui/Input.tsx"
      provides: "TextInput wrapper with label and theme-aware styling"
      exports: ["Input"]
    - path: "src/components/ui/Badge.tsx"
      provides: "Badge with color and variant props"
      exports: ["Badge"]
    - path: "src/components/ui/ProgressBar.tsx"
      provides: "Segmented progress bar (matches dashboard resource bars)"
      exports: ["ProgressBar"]
    - path: "src/components/ui/EmptyState.tsx"
      provides: "Empty state placeholder with icon and message"
      exports: ["EmptyState"]
    - path: "src/components/ui/index.ts"
      provides: "Barrel export of all UI components"
      exports: ["Button", "Card", "Input", "Badge", "ProgressBar", "EmptyState"]
    - path: "src/components/common/HudHeader.tsx"
      provides: "HUD-styled screen header with title and optional subtitle"
      exports: ["HudHeader"]
  key_links:
    - from: "src/components/ui/*.tsx"
      to: "src/theme/dark.ts"
      via: "useTheme() from @react-navigation/native"
      pattern: "useTheme.*from.*@react-navigation/native"
    - from: "src/components/ui/*.styles.ts"
      to: "src/constants/colors.ts"
      via: "COLORS import for static styles"
      pattern: "import.*COLORS.*from.*constants"
---

<objective>
Create reusable UI components (Button, Card, Input, Badge, ProgressBar, EmptyState) and common components (HudHeader) following the component + styles.ts separation pattern.

Purpose: Provide the building blocks that all screens will consume. Components must use theme hooks (not hardcoded colors) and follow zero border-radius HUD aesthetic.
Output: 7 UI components + 1 common component, each with separate styles file, barrel exports.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-y-estructura-visual/04-RESEARCH.md
@.planning/phases/04-design-system-y-estructura-visual/04-01-PLAN.md
@.planning/phases/04-design-system-y-estructura-visual/04-02-PLAN.md
@app/(tabs)/dashboard.tsx
@app/(auth)/login.tsx

<interfaces>
<!-- From Plan 01 (src/constants/): -->
```typescript
import { COLORS, SPACING, FONT_FAMILY, TYPOGRAPHY } from '@/src/constants';
```

<!-- From Plan 02 (src/theme/): -->
```typescript
import { useTheme } from '@react-navigation/native';
import { useThemeColor } from '@/src/hooks/use-theme-color';
import type { AppTheme } from '@/src/theme';
```

<!-- Theme interface from @react-navigation/native: -->
```typescript
interface Theme {
  dark: boolean;
  colors: Record<string, string>;
}
```

<!-- Types from Phase 2 (src/types-dtos/): -->
```typescript
import { ResourceItem } from '@/src/types-dtos';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Button, Card, and Input components with styles</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Button.styles.ts, src/components/ui/Card.tsx, src/components/ui/Card.styles.ts, src/components/ui/Input.tsx, src/components/ui/Input.styles.ts</files>
  <action>Create three UI component pairs (component + styles):

1. **Button** (`Button.tsx` + `Button.styles.ts`):
   - Props: `children`, `variant?: 'primary' | 'secondary' | 'ghost'`, `onPress`, `disabled?`, `fullWidth?`, `icon?`
   - Primary: transparent bg, cyan400 border, cyan400 text
   - Secondary: navy800 bg, navy700 border, gray100 text
   - Ghost: transparent bg, no border, cyan400 text
   - Zero border-radius (HUD principle)
   - Disabled state: gray500 border, gray500 text, 0.5 opacity
   - fullWidth stretches to container width
   - Use `useTheme()` for theme-aware colors in styles

2. **Card** (`Card.tsx` + `Card.styles.ts`):
   - Props: `children`, `title?`, `variant?: 'default' | 'alert' | 'discovery'`
   - Default: navy800 bg, navy700 border, 16px padding
   - Alert: navy800 bg, orange400_30 border
   - Discovery: navy800 bg, cyan400_20 border
   - Title renders in h4 typography style (cyan400, letter-spacing 3)
   - Zero border-radius

3. **Input** (`Input.tsx` + `Input.styles.ts`):
   - Props: `label`, `value`, `onChangeText`, `placeholder`, `secureTextEntry?`, `keyboardType?`, `multiline?`, `error?`
   - Label above input in label typography (cyan400, letter-spacing 2)
   - Input: navy800 bg, navy700 border, gray100 text, gray500 placeholder
   - Error state: red500 border, red500 error message below
   - Zero border-radius (explicitly set to override Android defaults per research pitfall #3)
   - Use `useThemeColor` for all colors

All styles files import COLORS from constants and use StyleSheet.create. Components import styles from their paired .styles.ts file. Per professor's pattern: component + styles.ts separation.</action>
  <verify>
    <automated>npx tsc --noEmit src/components/ui/Button.tsx src/components/ui/Card.tsx src/components/ui/Input.tsx</automated>
  </verify>
  <done>All six files exist, TypeScript compiles, Button has 3 variants, Card has 3 variants, Input has error state, zero border-radius on all components</done>
</task>

<task type="auto">
  <name>Task 2: Create Badge, ProgressBar, EmptyState, HudHeader and barrel exports</name>
  <files>src/components/ui/Badge.tsx, src/components/ui/Badge.styles.ts, src/components/ui/ProgressBar.tsx, src/components/ui/ProgressBar.styles.ts, src/components/ui/EmptyState.tsx, src/components/ui/EmptyState.styles.ts, src/components/ui/index.ts, src/components/common/HudHeader.tsx, src/components/common/HudHeader.styles.ts</files>
  <action>Create remaining components:

1. **Badge** (`Badge.tsx` + `Badge.styles.ts`):
   - Props: `label`, `color` (string), `variant?: 'solid' | 'outline' | 'ghost'`
   - Solid: bg = color (with opacity), text = color
   - Outline: transparent bg, border = color, text = color
   - Ghost: transparent bg, no border, text = color
   - Small pill shape but with zero border-radius (HUD style): use padding instead of borderRadius
   - Font: label typography

2. **ProgressBar** (`ProgressBar.tsx` + `ProgressBar.styles.ts`):
   - Props: `value` (0-100), `segments?: number` (default 10), `color?` (default cyan400), `criticalColor?` (default red500), `isCritical?`
   - Renders segmented bar matching dashboard's existing resource bar pattern (10 segments, gap 2px)
   - Active segments use color, inactive use navy700, critical uses criticalColor
   - Extract the exact segment rendering logic from dashboard.tsx lines 39-51 into reusable component

3. **EmptyState** (`EmptyState.tsx` + `EmptyState.styles.ts`):
   - Props: `icon` (string or ReactNode), `title`, `description?`, `actionLabel?`, `onAction?`
   - Centered layout, muted text, optional action button
   - Used when lists/queries return no data

4. **HudHeader** (`HudHeader.tsx` + `HudHeader.styles.ts` in common/):
   - Props: `title`, `subtitle?`, `rightAction?`
   - HUD-styled screen header: title in h2 typography (cyan400, letter-spacing 4), subtitle in caption style (gray500)
   - Use SafeAreaView for notch handling
   - Consistent across all screens

5. **Barrel export** (`src/components/ui/index.ts`):
   - `export { Button } from './Button'`
   - `export { Card } from './Card'`
   - `export { Input } from './Input'`
   - `export { Badge } from './Badge'`
   - `export { ProgressBar } from './ProgressBar'`
   - `export { EmptyState } from './EmptyState'`

Per research: ProgressBar should match the exact segmented pattern from dashboard.tsx — this is the component that dashboard will consume to replace its inline segment rendering.</action>
  <verify>
    <automated>npx tsc --noEmit src/components/ui/Badge.tsx src/components/ui/ProgressBar.tsx src/components/ui/EmptyState.tsx src/components/common/HudHeader.tsx src/components/ui/index.ts</automated>
  </verify>
  <done>All component files exist, TypeScript compiles, barrel export works, ProgressBar matches dashboard segment pattern, HudHeader uses SafeAreaView</done>
</task>

</tasks>

<verification>
- All 9 component files + 9 styles files + 1 barrel export exist
- TypeScript compiles without errors across all component files
- Button renders 3 variants (primary/secondary/ghost) with correct colors
- Card renders with HUD border and zero border-radius
- Input shows error state with red500 border
- Badge renders with color prop and 3 variants
- ProgressBar renders segmented bars matching dashboard's existing pattern
- EmptyState renders centered placeholder
- HudHeader renders title with HUD styling and safe area handling
- All components use useTheme/useThemeColor (no hardcoded colors)
- Barrel export allows `import { Button, Card, Input, Badge, ProgressBar, EmptyState } from '@/src/components/ui'`
</verification>

<success_criteria>
- src/components/ui/ has 6 components + 6 styles files + index.ts barrel export
- src/components/common/ has HudHeader component + styles file
- All components use theme hooks (useTheme/useThemeColor) instead of hardcoded colors
- Zero border-radius enforced on all UI components (HUD principle)
- ProgressBar component matches dashboard's existing segmented bar pattern exactly
- Barrel export enables clean single-line imports
- All files pass TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-system-y-estructura-visual/04-03-SUMMARY.md`
</output>
