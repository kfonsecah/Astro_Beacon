---
phase: 04-design-system-y-estructura-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/colors.ts
  - src/constants/spacing.ts
  - src/constants/typography.ts
  - src/constants/index.ts
autonomous: true
requirements:
  - DS-01
must_haves:
  truths:
    - "Color palette extracted from hardcoded screen values into typed constants"
    - "Spacing scale uses consistent 4px base increments"
    - "Typography defines font families with Platform.OS resolution for iOS/Android"
    - "All constants are exported via barrel file"
  artifacts:
    - path: "src/constants/colors.ts"
      provides: "Raw color palette with opacity variants"
      contains: "COLORS object with navy, gray, cyan, orange, green, red, purple, blue, amber"
    - path: "src/constants/spacing.ts"
      provides: "Spacing scale (4px base)"
      contains: "SPACING object with xxs through xxxx keys"
    - path: "src/constants/typography.ts"
      provides: "Font families and type scale"
      contains: "FONT_FAMILY with Platform.OS resolution, TYPOGRAPHY with h1-body-caption sizes"
    - path: "src/constants/index.ts"
      provides: "Barrel export of all constants"
      exports: ["COLORS", "SPACING", "FONT_FAMILY", "TYPOGRAPHY"]
  key_links:
    - from: "src/constants/typography.ts"
      to: "react-native Platform.OS"
      via: "Platform.OS === 'ios' ? 'IBM Plex Mono' : 'IBMPlexMono-Regular'"
      pattern: "Platform\\.OS"
---

<objective>
Create design token constants (colors, spacing, typography) extracted from existing screen styles.

Purpose: Establish the immutable foundation that themes and components will consume. No hardcoded colors should remain in screens after this phase completes.
Output: Four files in src/constants/ with typed, exported design tokens.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-y-estructura-visual/04-RESEARCH.md
@app/(auth)/login.tsx
@app/(tabs)/dashboard.tsx

<interfaces>
<!-- Hardcoded color values found in current screens that must be extracted to constants: -->
<!-- Background: #0B1120 (navy900), #111827 (navy800) -->
<!-- Borders: #1F2937 (navy700), #374151 (navy600) -->
<!-- Text primary: #E5E7EB (gray100), #9CA3AF (gray300) -->
<!-- Text secondary: #6B7280 (gray400), #4B5563 (gray500) -->
<!-- Accent: #6EE7B7 (cyan400), #FB923C (orange400) -->
<!-- Status: #22C55E (green500), #EF4444 (red500), #A855F7 (purple500), #3B82F6 (blue500), #F59E0B (amber500) -->
<!-- Opacity variants needed: cyan400_10/20/30, orange400_10/30, green500_10, red500_10 -->

<!-- Typography used in screens: fontFamily: "monospace" (must map to IBM Plex Mono / IBMPlexMono-Regular) -->
<!-- Font sizes: 8, 9, 10, 11, 12, 14, 16, 18, 28 -->
<!-- Letter spacing: 0, 1, 2, 3, 4, 6 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create color constants with opacity variants</name>
  <files>src/constants/colors.ts</files>
  <action>Create `src/constants/colors.ts` with a `COLORS` object using `as const`. Extract all hardcoded hex values from existing screens (login.tsx, dashboard.tsx, bestiary.tsx, resources.tsx, map.tsx, logbook.tsx). Include:

- Backgrounds: navy900 (#0B1120), navy800 (#111827), navy700 (#1F2937), navy600 (#374151)
- Text: gray100 (#E5E7EB), gray300 (#9CA3AF), gray400 (#6B7280), gray500 (#4B5563)
- Accent: cyan400 (#6EE7B7), orange400 (#FB923C)
- Status: green500 (#22C55E), red500 (#EF4444), purple500 (#A855F7), blue500 (#3B82F6), amber500 (#F59E0B)
- Opacity variants using rgba(): cyan400_10, cyan400_20, cyan400_30, orange400_10, orange400_30, green500_10, red500_10

Per D-01 (research decision): Use rgba() strings for opacity variants, not hex concatenation. Export as `export const COLORS = { ... } as const`.</action>
  <verify>
    <automated>npx tsc --noEmit src/constants/colors.ts</automated>
  </verify>
  <done>File exists with all color keys, opacity variants use rgba(), TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create spacing and typography constants</name>
  <files>src/constants/spacing.ts, src/constants/typography.ts, src/constants/index.ts</files>
  <action>Create three files:

1. `src/constants/spacing.ts` — SPACING object with 4px base scale: xxs(4), xs(8), sm(12), md(16), lg(20), xl(24), xxl(32), xxxl(40), xxxx(48). Export `as const`.

2. `src/constants/typography.ts` — FONT_FAMILY object with Platform.OS resolution: mono maps to 'IBM Plex Mono' on iOS, 'IBMPlexMono-Regular' on Android; monoBold, body (Space Grotesk), bodyBold similarly. TYPOGRAPHY object with type scale: h1(28/6/36), h2(18/4/24), h3(16/3/22), h4(14/3/20), label(11/2/16), data(12/1/18), body(14/0/20), caption(9/2/14), footer(8/1/12). Format: fontSize/letterSpacing/lineHeight.

3. `src/constants/index.ts` — Barrel export: `export { COLORS } from './colors'`, `export { SPACING } from './spacing'`, `export { FONT_FAMILY, TYPOGRAPHY } from './typography'`.

Per research pitfalls: Platform.OS resolution is critical for Android font loading. Do NOT use generic 'monospace' — use explicit font names.</action>
  <verify>
    <automated>npx tsc --noEmit src/constants/spacing.ts src/constants/typography.ts src/constants/index.ts</automated>
  </verify>
  <done>All three files exist, TypeScript compiles, FONT_FAMILY uses Platform.OS conditional, barrel export works</done>
</task>

</tasks>

<verification>
- All four files in src/constants/ exist and compile
- COLORS contains all hex values from existing screens plus rgba opacity variants
- SPACING uses 4px base scale (4, 8, 12, 16, 20, 24, 32, 40, 48)
- TYPOGRAPHY covers all font sizes used in screens (8-28px)
- FONT_FAMILY resolves correctly for both iOS and Android
- No hardcoded color values remain in constants files (they ARE the source of truth)
</verification>

<success_criteria>
- src/constants/ directory created with 4 files (colors.ts, spacing.ts, typography.ts, index.ts)
- All design tokens typed with `as const` for type inference
- Colors match existing screen palette exactly (no new colors introduced)
- Spacing scale covers all values used in current screens
- Typography scale covers all font sizes, letter spacings, and line heights from existing screens
- TypeScript compiles without errors across all constant files
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-system-y-estructura-visual/04-01-SUMMARY.md`
</output>
