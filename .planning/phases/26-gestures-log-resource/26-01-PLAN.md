---
phase: 26-gestures-log-resource
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/_layout.tsx, app/(tabs)/bestiary.tsx]
autonomous: true
requirements: [GEST-01, GEST-02]
---

<objective>
Configure the foundation for native gestures and implement the required gestures in the Bestiary screen.

Purpose: Enable interactive UX patterns (Swipe and Long-press) as per course requirements and project vision.
Output:
- GestureHandlerRootView integrated in the root layout.
- Swipe-left action in bestiary cards to navigate to details.
- Long-press in bestiary cards to show a quick preview modal.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-gestures-log-resource/26-CONTEXT.md
@.planning/phases/26-gestures-log-resource/26-RESEARCH.md
@app/_layout.tsx
@app/(tabs)/bestiary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GestureHandlerRootView to Root Layout</name>
  <files>app/_layout.tsx</files>
  <action>
    - Import `GestureHandlerRootView` from `react-native-gesture-handler`.
    - Wrap the entire return tree (starting from `QueryClientProvider` or just inside it) with `GestureHandlerRootView` set to `style={{ flex: 1 }}`.
    - This is critical for gestures to work on Android per Research Pitfall 1.
  </action>
  <verify>
    <automated>grep -q "GestureHandlerRootView" app/_layout.tsx</automated>
  </verify>
  <done>GestureHandlerRootView is present and wraps the main navigation stack.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Swipe-left and Long-press in Bestiary</name>
  <files>app/(tabs)/bestiary.tsx</files>
  <action>
    - Import `Swipeable` from `react-native-gesture-handler`.
    - Create a `renderRightActions` function that returns a `TouchableOpacity` (cyan background per D-04) with "VER" text.
    - Wrap the existing species card `TouchableOpacity` with `Swipeable`.
    - Pass `renderRightActions` to the `Swipeable` component.
    - Implement `onLongPress` on the card `TouchableOpacity` to set a `previewSpecies` state.
    - Add a `Modal` component (at the end of the screen) that displays `previewSpecies` details (Name, Classification, Danger Level) when set.
    - Ensure the Modal can be dismissed by tapping an overlay or a "CERRAR" button per D-08.
    - Add a small hint text under the Bestiary subtitle: "← Desliza | Mantén presionado para preview" per D-10.
  </action>
  <verify>
    <automated>grep -q "Swipeable" app/(tabs)/bestiary.tsx && grep -q "onLongPress" app/(tabs)/bestiary.tsx</automated>
  </verify>
  <done>Bestiary screen supports swipe-left to navigate and long-press for quick preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Navigation | Gestures trigger navigation to detail screens. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | bestiary.tsx | mitigate | Ensure ID passed to navigation is validated by the router/detail screen. |
</threat_model>

<verification>
1. Open the app and navigate to the Bestiary tab.
2. Swipe left on any species card; a cyan "VER" button should appear.
3. Tap the "VER" button; it should navigate to the species detail screen.
4. Long-press on any species card; a modal should appear with a quick preview.
5. Tap "CERRAR" or outside the modal; it should disappear.
</verification>

<success_criteria>
- Swipe-left gesture reveals "VER" action in bestiary.
- Long-press gesture shows preview modal in bestiary.
- GestureHandlerRootView correctly installed in _layout.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/26-gestures-log-resource/26-01-SUMMARY.md`
</output>
