---
phase: 26-gestures-log-resource
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: [src/services/resource.service.ts, app/log-resource/index.tsx, app/(tabs)/resources.tsx]
autonomous: true
requirements: [LOGR-01]
---

<objective>
Implement the Log Resource movement screen and fix the service layer DTO mismatch.

Purpose: Complete the resource management lifecycle by allowing users to record inflows and outflows of materials.
Output:
- resource.service.ts mapping Spanish keys to English keys for the API.
- New screen `app/log-resource/index.tsx` with a functional form.
- FAB button in `app/(tabs)/resources.tsx` to navigate to the new screen.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-gestures-log-resource/26-CONTEXT.md
@.planning/phases/26-gestures-log-resource/26-RESEARCH.md
@src/services/resource.service.ts
@app/(tabs)/resources.tsx
@api/src/schemas/resource.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DTO Mapping and Path in Resource Service</name>
  <files>src/services/resource.service.ts</files>
  <action>
    - In `recordMovement`, map the `data` (CreateRecursoMovimientoDTO) to the format expected by the backend:
      - `type`: `data.tipo`
      - `amount`: `data.cantidad`
      - `notes`: `data.razon`
    - Update the endpoint path from `/resources/${resourceId}/movement` to `/resources/${resourceId}/movements` (plural per backend route).
  </action>
  <verify>
    <automated>grep -q "movements" src/services/resource.service.ts && grep -q "type: data.tipo" src/services/resource.service.ts</automated>
  </verify>
  <done>Resource service correctly formats data for the backend API.</done>
</task>

<task type="auto">
  <name>Task 2: Create Log Resource Screen</name>
  <files>app/log-resource/index.tsx</files>
  <action>
    - Create a new screen in `app/log-resource/index.tsx`.
    - Implement a form using `useResources` (to populate a resource selector) and `useRecordResourceMovement` hook.
    - Fields:
      - Resource Selector (Dropdown or Chip list).
      - Movement Type: "INGRESO" | "EGRESO" (Buttons/Chips).
      - Amount (Numeric input).
      - Reason (Text input).
      - Trip Link (Optional/Text input per D-18).
    - Styling: Follow design system (use `tc.surface`, `tc.primary`, etc.).
    - On success: Navigate back using `router.back()`.
  </action>
  <verify>
    <automated>ls app/log-resource/index.tsx</automated>
  </verify>
  <done>Log Resource screen is functional and connected to the API.</done>
</task>

<task type="auto">
  <name>Task 3: Add Navigation to Log Resource from Resources Tab</name>
  <files>app/(tabs)/resources.tsx</files>
  <action>
    - Add a floating action button (FAB) or a prominent button in the header area.
    - Title: "REGISTRAR MOVIMIENTO" or a "+" icon.
    - Navigate to `/log-resource` on press.
  </action>
  <verify>
    <automated>grep -q "/log-resource" app/(tabs)/resources.tsx</automated>
  </verify>
  <done>User can navigate to the Log Resource screen from the inventory list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Movement data sent to backend for inventory update. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering | log-resource/index.tsx | mitigate | Client-side validation for non-negative amount before submission. |
</threat_model>

<verification>
1. Open the app and navigate to the Resources tab.
2. Tap the "REGISTRAR MOVIMIENTO" button; it should open the Log Resource screen.
3. Select a resource, choose "EGRESO", enter an amount and a reason.
4. Tap "REGISTRAR".
5. The app should return to the Resources screen, and the stock for that resource should be updated (decreased).
</verification>

<success_criteria>
- Resource movements can be recorded through the new screen.
- Service layer correctly maps fields (tipo -> type, etc.).
- Navigation from Resources to Log Resource works as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/26-gestures-log-resource/26-02-SUMMARY.md`
</output>
