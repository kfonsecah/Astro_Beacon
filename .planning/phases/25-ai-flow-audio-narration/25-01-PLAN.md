# Phase 25 Plan 01: Service & Hook Layer Update

Integration of AI identification service and hooks for the mobile frontend.

## Proposed Changes

### DTOs
#### [src/types-dtos/especie.dto.ts]
- Add `IdentifyResult` interface as per D-03.

### Service Layer
#### [src/services/species.service.ts]
- Add `identify(imageBase64: string)` method to call `POST /species/identify`.

### Hook Layer
#### [src/hooks/useSpecies.ts]
- Add `useIdentifySpecies` mutation hook.

## Verification Plan

### Automated Tests
- N/A for this plan (direct service/hook additions).

### Manual Verification
- Verify that `useIdentifySpecies` can be imported and initialized in a component.
- Verify that the service correctly calls the backend endpoint (can be checked in the next plan during integration).
