---
phase: 24
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified: [app/_layout.tsx, app/(tabs)/bestiary.tsx, app/species/identify.tsx]
autonomous: true
requirements: [AI-06]
must_haves:
  truths:
    - "FAB in bestiary screen opens the identification modal"
    - "User can capture or pick an image and see its preview"
    - "Form submission sends compressed base64 to the backend"
  artifacts:
    - path: "app/species/identify.tsx"
      provides: "Species identification modal/form"
  key_links:
    - from: "app/species/identify.tsx"
      to: "useImagePicker"
      via: "hook usage for image capture"
    - from: "app/species/identify.tsx"
      to: "useCreateSpecies"
      via: "form submission"
---

<objective>
Implementar el modal de identificación de especies, permitiendo la captura de imágenes procesadas y el registro manual de datos mediante un formulario integrado con el backend.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-species-detail-camera/24-CONTEXT.md
@.planning/phases/24-species-detail-camera/24-RESEARCH.md
@app/_layout.tsx
@app/(tabs)/bestiary.tsx
@src/hooks/useImagePicker.ts
@src/hooks/useSpecies.ts
</context>

<tasks>

<task type="auto">
  <name>Configurar ruta modal y disparador (FAB)</name>
  <files>app/_layout.tsx, app/(tabs)/bestiary.tsx</files>
  <action>
    1. En `app/_layout.tsx`, agregar una entrada para `species/identify` dentro del `Stack` protegido con `options={{ presentation: 'modal', headerShown: false }}`.
    2. En `app/(tabs)/bestiary.tsx`, implementar un FAB (Floating Action Button) de estilo HUD:
       - Posicionado en la esquina inferior derecha.
       - Fondo `tc.primary`, icono de cámara o "plus".
       - Al presionar, navegar a `/species/identify`.
  </action>
  <verify>
    <automated>grep "species/identify" app/_layout.tsx && grep "router.push('/species/identify')" app/(tabs)/bestiary.tsx</automated>
  </verify>
  <done>Ruta modal configurada y FAB de acceso implementado en el bestiario.</done>
</task>

<task type="auto">
  <name>Crear pantalla app/species/identify.tsx</name>
  <files>app/species/identify.tsx</files>
  <action>
    Implementar el modal de identificación (per AI-06):
    1. Estructura: `SafeAreaView` con fondo `tc.background`.
    2. Header: "IDENTIFICACIÓN DE ESPECIE".
    3. Área de Imagen:
       - Mostrar preview si hay imagen cargada.
       - Botones para "TOMAR FOTO" y "SELECCIONAR GALERÍA" usando el hook `useImagePicker`.
    4. Formulario de Datos:
       - Campos `name`, `classification`, `dangerLevel`, `description`, `notes`.
       - Usar `src/components/ui/Input.tsx` para textos.
       - Usar un sistema simple de selección (e.g. chips o botones) para Enums (Classification y Danger Level).
    5. Integración:
       - Usar `useCreateSpecies` para enviar los datos al servidor al presionar "REGISTRAR".
       - Al tener éxito, cerrar el modal con `router.back()`.
    6. Validación: Asegurar que al menos el nombre y la imagen (base64) estén presentes antes de habilitar el botón de registro.
  </action>
  <verify>
    <automated>ls app/species/identify.tsx && grep "useImagePicker" app/species/identify.tsx && grep "useCreateSpecies" app/species/identify.tsx</automated>
  </verify>
  <done>Pantalla de identificación creada con formulario completo y captura de imagen integrada.</done>
</task>

</tasks>

<success_criteria>
- El modal se abre correctamente desde el FAB del bestiario.
- El usuario puede capturar/seleccionar una imagen y ver la preview.
- El formulario permite ingresar todos los campos requeridos.
- El registro de la especie se guarda exitosamente en el backend y el modal se cierra.
</success_criteria>

<output>
After completion, create .planning/phases/24-species-detail-camera/24-03-SUMMARY.md
</output>
