---
phase: 24
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [app/species/[id].tsx, app/(tabs)/bestiary.tsx, app/_layout.tsx]
autonomous: true
requirements: [UI-09]
---

<objective>
Implementar la pantalla de detalle de especie para visualizar la información completa de un registro y habilitar la navegación desde el bestiario planetario.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-species-detail-camera/24-CONTEXT.md
@.planning/phases/24-species-detail-camera/24-RESEARCH.md
@app/(tabs)/bestiary.tsx
@app/_layout.tsx
@src/hooks/useSpecies.ts
</context>

<tasks>

<task type="auto">
  <name>Registrar ruta en Root Layout</name>
  <files>app/_layout.tsx</files>
  <action>
    Registrar la ruta `species/[id]` en `app/_layout.tsx` dentro del bloque `Stack.Protected` para asegurar que solo usuarios autenticados puedan ver el detalle.
    Configuración: `<Stack.Screen name="species/[id]" options={{ headerShown: false }} />`.
  </action>
  <verify>
    <automated>grep "species/\[id\]" app/_layout.tsx</automated>
  </verify>
  <done>Ruta de detalle registrada en el stack protegido.</done>
</task>

<task type="auto">
  <name>Crear pantalla app/species/[id].tsx</name>
  <files>app/species/[id].tsx</files>
  <action>
    Implementar la pantalla de detalle de especie (per UI-09):
    1. Obtener el `id` de los parámetros de búsqueda.
    2. Usar el hook `useSpeciesById(id)` para obtener los datos.
    3. Mostrar un estado de carga (ActivityIndicator) y error si fallan.
    4. Estética HUD:
       - Header con el nombre de la especie en mayúsculas (usar `HudHeader` o similar).
       - Imagen de la especie usando `expo-image` con un placeholder si no existe.
       - Sección de Atributos: Clasificación y Nivel de Peligro (usar los mapas de colores de `bestiary.tsx`).
       - Sección de Biometría/IA: Mostrar confianza de la IA y fecha de descubrimiento.
       - Descripción detallada.
       - Campo de **Notas Adicionales** (`notes`): Mostrar solo si existe contenido.
       - Botón de retroceso usando `router.back()`.
    5. Utilizar tokens de `useTheme` para todos los colores (evitar hex hardcodeados).
  </action>
  <verify>
    <automated>ls app/species/[id].tsx && grep "useSpeciesById" app/species/[id].tsx && grep "notes" app/species/[id].tsx</automated>
  </verify>
  <done>Pantalla de detalle creada con cumplimiento del Design System HUD y visualización de notas.</done>
</task>

<task type="auto">
  <name>Integrar navegación en BestiaryScreen</name>
  <files>app/(tabs)/bestiary.tsx</files>
  <action>
    Actualizar `renderItem` en `app/(tabs)/bestiary.tsx`:
    1. Envolver el contenido del item en un `TouchableOpacity` (o similar).
    2. Implementar `onPress` para navegar a `app/species/[id].tsx` pasando el `id` de la especie.
    3. Importar `useRouter` de `expo-router`.
  </action>
  <verify>
    <automated>grep "router.push" app/(tabs)/bestiary.tsx</automated>
  </verify>
  <done>Navegación al detalle integrada en el listado de especies.</done>
</task>

</tasks>

<success_criteria>
- La ruta `species/[id]` está registrada correctamente en `app/_layout.tsx`.
- La pantalla `app/species/[id].tsx` renderiza correctamente los datos de la especie, incluyendo el campo `notes`.
- La navegación desde el listado del bestiario hacia el detalle funciona según lo esperado.
- Se respetan los colores y estilos HUD establecidos.
</success_criteria>

<output>
After completion, create .planning/phases/24-species-detail-camera/24-02-SUMMARY.md
</output>
