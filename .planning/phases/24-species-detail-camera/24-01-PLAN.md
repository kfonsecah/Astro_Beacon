---
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, app.json, src/types-dtos/especie.dto.ts, src/hooks/useImagePicker.ts, src/utils/image.test.ts, app/species/[id].test.tsx]
autonomous: true
requirements: [AI-04, AI-05]
must_haves:
  truths:
    - "App declares camera and photo gallery permissions in manifest"
    - "CreateEspecieDTO supports name, classification, danger level, description, and notes"
    - "Image processing logic applies 800px resize and 0.4 quality compression"
  artifacts:
    - path: "src/hooks/useImagePicker.ts"
      provides: "Image capture and compression logic"
    - path: "src/utils/image.test.ts"
      provides: "Unit tests for image compression"
    - path: "app/species/[id].test.tsx"
      provides: "Component tests for detail rendering"
  key_links:
    - from: "src/hooks/useImagePicker.ts"
      to: "expo-image-manipulator"
      via: "manipulateAsync call"
---

<objective>
Configurar la infraestructura necesaria para la captura y procesamiento de imágenes, incluyendo dependencias, permisos en el manifest de la app y la actualización de los DTOs del frontend para soportar el formulario completo de especies. Además, establecer el andamiaje de pruebas para la fase.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-species-detail-camera/24-CONTEXT.md
@.planning/phases/24-species-detail-camera/24-RESEARCH.md
@.planning/phases/24-species-detail-camera/24-VALIDATION.md
@app.json
@package.json
@src/types-dtos/especie.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Crear stubs de pruebas (Wave 0)</name>
  <files>src/utils/image.test.ts, app/species/[id].test.tsx</files>
  <action>
    Crear los archivos de prueba requeridos por VALIDATION.md:
    1. `src/utils/image.test.ts`: Crear un test que verifique la lógica de configuración de compresión (mocking expo-image-manipulator si es necesario para validar parámetros).
    2. `app/species/[id].test.tsx`: Crear un test de renderizado básico que verifique que los campos obligatorios se muestran en la pantalla de detalle.
  </action>
  <verify>
    <automated>ls src/utils/image.test.ts app/species/[id].test.tsx</automated>
  </verify>
  <done>Archivos de prueba creados.</done>
</task>

<task type="auto">
  <name>Instalar dependencias y configurar app.json</name>
  <files>package.json, app.json</files>
  <action>
    1. Instalar `expo-image-picker` (~16.0.x) y `expo-image-manipulator` (~9.0.x) usando `npx expo install`.
    2. Configurar el plugin `expo-image-picker` en `app.json` con los mensajes de permiso requeridos para Cámara y Galería de Fotos (per AI-04).
    Mensajes sugeridos:
    - cameraPermission: "Astro Beacon necesita acceso a la cámara para identificar nuevas especies."
    - photosPermission: "Astro Beacon necesita acceso a tu galería para cargar imágenes de especies."
  </action>
  <verify>
    <automated>grep -E "expo-image-picker|expo-image-manipulator" package.json && grep "expo-image-picker" app.json</automated>
  </verify>
  <done>Dependencias instaladas y permisos declarados en app.json.</done>
</task>

<task type="auto">
  <name>Actualizar CreateEspecieDTO</name>
  <files>src/types-dtos/especie.dto.ts</files>
  <action>
    Actualizar la interfaz `CreateEspecieDTO` para incluir los campos requeridos por el formulario de identificación (per AI-06 y validación con backend schema):
    - `name: string`
    - `classification: SpeciesClassification`
    - `dangerLevel: DangerLevel`
    - `description: string` (ya existe, asegurar que sea obligatoria o coherente)
    - `notes?: string`
    - `imageUrl?: string` (ya existe)
    - `location?: { lat: number; lng: number }` (ya existe)
  </action>
  <verify>
    <automated>grep -E "name:|classification:|dangerLevel:|notes\?:" src/types-dtos/especie.dto.ts</automated>
  </verify>
  <done>DTO actualizado para soportar el envío completo de datos de la especie.</done>
</task>

<task type="auto">
  <name>Crear hook useImagePicker</name>
  <files>src/hooks/useImagePicker.ts</files>
  <action>
    Implementar un hook personalizado que envuelva la lógica de `expo-image-picker` y `expo-image-manipulator`:
    1. Método `pickImage`: Abre la galería.
    2. Método `takePhoto`: Abre la cámara.
    3. Lógica de procesamiento: Después de obtener la imagen, usar `ImageManipulator` para redimensionar a `width: 800` y comprimir con `quality: 0.4` (per AI-05).
    4. El hook debe devolver el `base64` resultante con el prefijo `data:image/jpeg;base64,`.
  </action>
  <verify>
    <automated>npm test src/utils/image.test.ts</automated>
  </verify>
  <done>Hook useImagePicker implementado con lógica de compresión según requerimientos, verificado por pruebas unitarias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Hardware -> App | El acceso a la cámara/galería requiere consentimiento explícito del usuario. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | Camera/Photos | mitigate | Declarar permisos en app.json y solicitar en tiempo de ejecución. |
| T-24-02 | Denial of Service | Image Upload | mitigate | Comprimir imagen a ~400KB (800px, 0.4 quality) antes de enviar. |
</threat_model>

<success_criteria>
- Dependencias `expo-image-picker` y `expo-image-manipulator` instaladas.
- Permisos configurados en `app.json`.
- `CreateEspecieDTO` contiene todos los campos necesarios.
- `useImagePicker` implementa correctamente la compresión a 800px y 0.4 quality.
- Pruebas unitarias para compresión de imagen pasando.
</success_criteria>

<output>
After completion, create .planning/phases/24-species-detail-camera/24-01-SUMMARY.md
</output>
