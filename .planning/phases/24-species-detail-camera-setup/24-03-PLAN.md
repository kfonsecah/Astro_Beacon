---
phase: 24
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified: [app/_layout.tsx, app/(tabs)/bestiary.tsx, app/species/identify.tsx]
autonomous: true
requirements: [AI-06]
must_haves:
  truths:
    - "FAB in bestiary screen opens the identification modal"
    - "User can capture or pick an image and see its preview"
    - "Form submission sends compressed base64 to the backend"
  artifacts:
    - path: "app/species/identify.tsx"
      provides: "Species identification modal/form"
  key_links:
    - from: "app/species/identify.tsx"
      to: "useImagePicker"
      via: "hook usage for image capture"
    - from: "app/species/identify.tsx"
      to: "useCreateSpecies"
      via: "form submission"
---

<objective>
Implementar el modal de identificación de especies, permitiendo la captura de imágenes procesadas y el registro manual de datos mediante un formulario integrado con el backend.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-species-detail-camera-setup/24-CONTEXT.md
@.planning/phases/24-species-detail-camera-setup/24-RESEARCH.md
@app/_layout.tsx
@app/(tabs)/bestiary.tsx
@src/hooks/useImagePicker.ts
@src/hooks/useSpecies.ts
</context>

<tasks>

<task type="auto">
  <name>Configurar ruta modal y FAB</name>
  <files>app/_layout.tsx, app/(tabs)/bestiary.tsx</files>
  <action>
    1. En `app/_layout.tsx`, agregar entrada para `species/identify` con `options={{ presentation: 'modal', headerShown: false }}` (per D-13, D-14).
    2. En `app/(tabs)/bestiary.tsx`, implementar FAB flotante de estilo HUD (per D-14):
       - Posicionado en esquina inferior derecha sobre la FlatList.
       - Estética HUD: zero border-radius, fondo `tc.primary`.
       - Al presionar: `router.push('/species/identify')`.
  </action>
  <verify>
    <automated>grep "species/identify" app/_layout.tsx && grep "router.push('/species/identify')" app/(tabs)/bestiary.tsx</automated>
  </verify>
  <done>Ruta modal configurada y FAB de acceso implementado en el bestiario.</done>
</task>

<task type="auto">
  <name>Crear pantalla app/species/identify.tsx</name>
  <files>app/species/identify.tsx</files>
  <action>
    Implementar el modal de identificación (per AI-06, D-13 a D-17):
    1. Estructura: `KeyboardAvoidingView` + `ScrollView` envolviendo `SafeAreaView` con fondo `tc.background`, para que el teclado no tape los campos del formulario.
    2. Header: "IDENTIFICACIÓN DE ESPECIE" via `HudHeader`.
    3. Área de imagen (per D-15):
       - Preview de imagen capturada, o placeholder HUD "SIN IMAGEN" si no hay.
       - Botones "CÁMARA" y "GALERÍA" usando `useImagePicker`:
         - "CÁMARA" → `pickFromCamera()`
         - "GALERÍA" → `pickFromGallery()`
    4. Botón "IDENTIFICAR CON IA" — disabled en esta fase (per D-15, habilitado en Phase 25).
    5. Formulario con chips HUD (per D-16 — no Picker nativo):
       - `name`: Input de texto (obligatorio).
       - `classification`: Chips en fila con `flexWrap: 'wrap'` para evitar overflow (botones tipo chip, zero border-radius).
       - `dangerLevel`: Chips en fila con `flexWrap: 'wrap'`.
       - `notes`: Input multiline (opcional).
       - Nota: `description` NO se incluye en el formulario de esta fase — es opcional en el DTO.
    6. Validación antes de submit: "GUARDAR ESPECIE" permanece disabled si `name` está vacío O si `classification` no está seleccionado O si `dangerLevel` no está seleccionado.
    7. Botón "GUARDAR ESPECIE" — al presionar: deshabilitar botón durante la mutación (prevenir doble submit), llamar a `useCreateSpecies` con datos + `image?.base64` como `imageUrl` (per D-15). Mostrar feedback de error en caso de fallo (Alert HUD con mensaje).
    8. Tras éxito: `router.back()` (per D-17). La invalidación de cache ya la maneja `useCreateSpecies`.
    9. Cero hex hardcodeados — usar `useTheme()` + `tc.*` en todo (per D-18, D-19).
  </action>
  <verify>
    <automated>ls app/species/identify.tsx && grep "pickFromCamera\|pickFromGallery" app/species/identify.tsx && grep "useCreateSpecies" app/species/identify.tsx</automated>
  </verify>
  <done>Pantalla de identificación creada con formulario completo, chips HUD, y captura de imagen integrada.</done>
</task>

</tasks>

<success_criteria>
- El modal se abre correctamente desde el FAB del bestiario.
- El usuario puede capturar imagen (CÁMARA) o seleccionar de galería (GALERÍA) y ver la preview.
- El formulario permite ingresar nombre, clasificación (chips con wrap), nivel de peligro (chips con wrap) y notas.
- "GUARDAR ESPECIE" está disabled si faltan campos obligatorios (name, classification, dangerLevel).
- "GUARDAR ESPECIE" se deshabilita durante la mutación (previene doble submit).
- Feedback de error visible si la mutación falla.
- El botón "IDENTIFICAR CON IA" existe pero está disabled.
- El registro de la especie se guarda exitosamente y el modal se cierra.
- `KeyboardAvoidingView` + `ScrollView` aseguran usabilidad en pantallas pequeñas.
- Cero hex hardcodeados en todo el archivo.
</success_criteria>

<output>
After completion, create .planning/phases/24-species-detail-camera-setup/24-03-SUMMARY.md
</output>
