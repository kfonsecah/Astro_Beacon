---
phase: 24
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [app/species/[id].tsx, app/(tabs)/bestiary.tsx, app/_layout.tsx]
autonomous: true
requirements: [UI-09]
---

<objective>
Implementar la pantalla de detalle de especie para visualizar la información completa de un registro y habilitar la navegación desde el bestiario planetario.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24-species-detail-camera-setup/24-CONTEXT.md
@.planning/phases/24-species-detail-camera-setup/24-RESEARCH.md
@app/(tabs)/bestiary.tsx
@app/_layout.tsx
@src/hooks/useSpecies.ts
</context>

<tasks>

<task type="auto">
  <name>Registrar ruta en Root Layout</name>
  <files>app/_layout.tsx</files>
  <action>
    Registrar la ruta `species/[id]` en `app/_layout.tsx` dentro del bloque `Stack.Protected` para asegurar que solo usuarios autenticados puedan ver el detalle.
    Configuración: `<Stack.Screen name="species/[id]" options={{ headerShown: false }} />`.
  </action>
  <verify>
    <automated>grep "species/\[id\]" app/_layout.tsx</automated>
  </verify>
  <done>Ruta de detalle registrada en el stack protegido.</done>
</task>

<task type="auto">
  <name>Crear pantalla app/species/[id].tsx</name>
  <files>app/species/[id].tsx</files>
  <action>
    Implementar la pantalla de detalle de especie (per UI-09, D-07 a D-12):
    1. Obtener el `id` con `useLocalSearchParams()` de expo-router. Normalizar: `const id = Array.isArray(rawId) ? rawId[0] : rawId`. Si `id` es `undefined` o vacío, mostrar estado de error "Especie no encontrada".
    2. Usar el hook `useSpeciesById(id)` para obtener los datos (per D-08).
    3. Mostrar estado de carga (ActivityIndicator) y error si fallan. Incluir opción de retry en error state.
    4. Estética HUD (per D-18, D-19 — cero hex hardcodeados, usar `useTheme()` + `tc.*`):
       - Header con nombre de la especie en mayúsculas (usar `HudHeader`).
       - Imagen: `<Image source={{ uri: species.imageUrl }}>` si existe `imageUrl` (acepta base64 data URI y URL remota). Si no hay imagen, mostrar placeholder HUD (rectángulo `tc.surface` con texto "SIN IMAGEN").
       - Badges de clasificación y nivel de peligro usando `tc.*` tokens (no hex mapeados de bestiary.tsx).
       - Confianza IA solo si existe (`species.confidence`); mostrar `--` si no hay valor.
       - Descripción: mostrar `--` si está vacía.
       - Notas: mostrar sección solo si `species.notes` tiene contenido (per D-09).
       - Botón "NARRAR" — disabled/placeholder en esta fase (funcional en Phase 25, per D-11).
    5. Botón de retroceso con `router.back()`.
  </action>
  <verify>
    <automated>npm test app/species/[id].test.tsx</automated>
  </verify>
  <done>Pantalla de detalle creada con cumplimiento del Design System HUD y visualización de notas, verificado por pruebas de componentes.</done>
</task>

<task type="auto">
  <name>Integrar navegación en BestiaryScreen</name>
  <files>app/(tabs)/bestiary.tsx</files>
  <action>
    Actualizar `app/(tabs)/bestiary.tsx` (per D-12):
    1. En el `renderItem` de la FlatList, añadir `onPress={() => router.push('/species/' + item.id)}` en cada card.
    2. Importar `useRouter` de `expo-router`.
  </action>
  <verify>
    <automated>grep "router.push" app/(tabs)/bestiary.tsx</automated>
  </verify>
  <done>Navegación al detalle integrada en el listado de especies.</done>
</task>

</tasks>

<success_criteria>
- La ruta `species/[id]` está registrada correctamente en `app/_layout.tsx`.
- La pantalla `app/species/[id].tsx` renderiza correctamente todos los campos incluyendo imagen (base64 y URL remota), badges, notas condicionales y botón NARRAR disabled.
- `id` normalizado — no crashea si es array o undefined.
- Placeholders HUD para imagen ausente, confianza ausente y descripción vacía.
- La navegación desde el listado del bestiario hacia el detalle funciona.
- Cero hex hardcodeados — todos los colores via `useTheme()`.
</success_criteria>

<output>
After completion, create .planning/phases/24-species-detail-camera-setup/24-02-SUMMARY.md
</output>
