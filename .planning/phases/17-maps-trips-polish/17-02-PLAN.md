---
phase: 17-maps-trips-polish
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - "app/(tabs)/map.tsx"
  - "package.json"
autonomous: true
requirements: []
must_haves:
  truths:
    - "Map view renders actual map with react-native-maps"
    - "Supply markers displayed on map with status colors"
    - "User GPS position shown on map"
    - "react-native-maps and expo-location installed"
  artifacts:
    - path: "app/(tabs)/map.tsx"
      provides: "MapView with supply markers and user location"
      contains: "MapView from react-native-maps"
    - path: "package.json"
      provides: "Dependencies for map functionality"
      contains: "react-native-maps"
  key_links:
    - from: "app/(tabs)/map.tsx"
      to: "useSupplies"
      via: "hook call to get supply data"
      pattern: "useSupplies\\("
    - from: "app/(tabs)/map.tsx"
      to: "MapView"
      via: "import and render"
      pattern: "import.*MapView.*from.*react-native-maps"
---

<objective>
Implement map view with react-native-maps showing supply markers, user GPS position, and status-based coloring.

Purpose: Replace placeholder map with interactive map showing supply drop locations.
Output: Working map view with markers, user location, and proper styling.
</objective>

<execution_context>
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Kendall Fonseca/Desktop/U/Moviles/Astro_Beacon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-maps-trips-polish/17-CONTEXT.md
@.planning/phases/17-maps-trips-polish/17-01-SUMMARY.md

@app/(tabs)/map.tsx
@src/hooks/useSupplies.ts
@src/types-dtos/suministro.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-native-maps and expo-location</name>
  <files>package.json</files>
  <action>
    Install required dependencies for map functionality (per D-01, D-02, D-09):
    
    1. Run: `npx expo install react-native-maps expo-location`
    2. Verify installation by checking package.json for new dependencies
    3. Note: react-native-maps may require additional setup for iOS/Android:
       - iOS: No additional setup needed with Expo
       - Android: Verify Google Maps API key is configured in app.json if needed
    4. Check if any additional configuration is needed in app.json (typically not for Expo managed workflow)
  </action>
  <verify>
    <automated>npm list react-native-maps expo-location 2>&1 | grep -E "react-native-maps|expo-location" || echo "INSTALLATION CHECK FAILED"</automated>
  </verify>
  <done>react-native-maps and expo-location added to package.json, installation verified</done>
</task>

<task type="auto">
  <name>Task 2: Implement MapView with supply markers and user location</name>
  <files>app/(tabs)/map.tsx</files>
  <action>
    Rewrite map.tsx to use react-native-maps with supply markers and user GPS position (per D-01, D-02, D-03):
    
    1. Import required modules:
       ```typescript
       import MapView, { Marker, PROVIDER_GOOGLE, type Region } from 'react-native-maps';
       import * as Location from 'expo-location';
       import { useEffect, useState } from 'react';
       ```
    
    2. Add state for user location and map region:
       ```typescript
       const [userLocation, setUserLocation] = useState<{ lat: number; lng: number } | null>(null);
       const [region, setRegion] = useState<Region>({
         latitude: -12.0464, // Default to Lima, Peru (or use a sensible default)
         longitude: -77.0428,
         latitudeDelta: 0.0922,
         longitudeDelta: 0.0421,
       });
       ```
    
    3. Request location permissions and get user location on mount:
       ```typescript
       useEffect(() => {
         (async () => {
           let { status } = await Location.requestForegroundPermissionsAsync();
           if (status !== 'granted') {
             console.warn('Permission to access location was denied');
             return;
           }
           
           let location = await Location.getCurrentPositionAsync({});
           const lat = location.coords.latitude;
           const lng = location.coords.longitude;
           setUserLocation({ lat, lng });
           setRegion({
             latitude: lat,
             longitude: lng,
             latitudeDelta: 0.0922,
             longitudeDelta: 0.0421,
           });
         })();
       }, []);
       ```
    
    4. Replace placeholder map View (lines 93-98) with MapView:
       ```tsx
       <View style={{ height: 300, marginBottom: 20 }}>
         <MapView
           provider={PROVIDER_GOOGLE}
           style={{ flex: 1 }}
           region={region}
           showsUserLocation={true}
           showsMyLocationButton={true}
         >
           {supplies.map((supply) => (
             <Marker
               key={supply.id}
               coordinate={{
                 latitude: supply.location.lat,
                 longitude: supply.location.lng,
               }}
               pinColor={statusColorMap[supply.status] || '#666'}
               title={`Supply ${supply.id.slice(-4)}`}
               description={`Status: ${supply.status}`}
             />
           ))}
         </MapView>
       </View>
       ```
    
    5. Keep the supply list FlatList below the map
    6. Update formatContents() call to use the fixed version (returns string now)
    7. Add CategoryLegend component above the supply list
    8. Verify no TypeScript errors
    
    Note: Per D-02, route calculation from user position to supply locations is mentioned but actual route calculation (Polyline) can be added later - the markers show where supplies are.
  </action>
  <verify>
    <automated>tsc --noEmit app/(tabs)/map.tsx 2>&1 | grep -i "error" || echo "No TypeScript errors"</automated>
  </verify>
  <done>MapView renders with supply markers (colored by status), user location shown, CategoryLegend displayed, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- [ ] react-native-maps and expo-location installed
- [ ] MapView component renders with Google provider
- [ ] Supply markers displayed at correct lat/lng with status colors
- [ ] User location shown on map (blue dot)
- [ ] CategoryLegend integrated below map
- [ ] No TypeScript errors
</verification>

<success_criteria>
- Map shows actual map tiles (not placeholder)
- Supply markers visible at correct locations
- User's GPS position displayed
- Map is interactive (can pan/zoom)
- Status colors match statusColorMap (pendiente=amber, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/17-maps-trips-polish/17-02-SUMMARY.md`
</output>
