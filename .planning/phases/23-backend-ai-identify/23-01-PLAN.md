---
phase: 23-backend-ai-identify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/schemas/species.schema.ts
  - api/src/services/species.service.ts
  - api/src/controllers/species.controller.ts
  - api/src/routes/species.routes.ts
autonomous: true
requirements:
  - AI-01
  - AI-03
must_haves:
  truths:
    - "El backend acepta imágenes en base64 para identificación con límite de 400KB."
    - "La API de Vision es llamada de forma segura con logging de errores (console.warn)."
    - "Existe un fallback determinístico si Google Cloud Vision falla o no está configurado."
  artifacts:
    - path: "api/src/schemas/species.schema.ts"
      provides: "Zod schemas con validación de tamaño (max 533333 chars) y enum DangerLevel.desconocido"
    - path: "api/src/services/species.service.ts"
      provides: "Método identify con supresión de errores y logging preventivo"
    - path: "api/src/controllers/species.controller.ts"
      provides: "Controlador retornando el sobre estándar { success, data }"
    - path: "api/src/routes/species.routes.ts"
      provides: "Configuración de enrutamiento prioritario para /identify"
  key_links:
    - from: "api/src/routes/species.routes.ts"
      to: "api/src/controllers/species.controller.ts"
      via: "Ruta POST /identify evaluada antes que GET /:id"
---

<objective>
Crear ruta `/identify`, controlador, y servicio con Google Cloud Vision API + fallback robusto.
Purpose: Permitir a los astronautas identificar especies desconocidas a partir de fotos (base64) enviadas desde la cámara del dispositivo móvil.
Output: Endpoint `/api/v1/species/identify` funcionando con Google Cloud Vision, validación de tamaño de payload y fallback determinístico.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/src/routes/species.routes.ts
@api/src/controllers/species.controller.ts
@api/src/services/species.service.ts
@api/src/schemas/species.schema.ts
@api/src/models/species.model.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Actualizar esquema Zod (Validation & Enum Sync)</name>
  <files>api/src/schemas/species.schema.ts</files>
  <action>
    1. Añadir `'desconocido'` al `dangerLevelEnum` de Zod para sincronizar con el modelo Mongoose.
    2. Modificar `createSpeciesSchema` y `updateSpeciesSchema` para relajar la validación de `imageUrl`: remover `.url()` y permitir `z.string().optional().or(z.literal(''))`.
    3. Crear `identifySpeciesSchema` con:
       `{ imageBase64: z.string().min(1, 'Base64 image is required').max(533333, 'Image too large (max 400KB)') }`.
  </action>
  <verify>
    <automated>grep -E "desconocido|max\(533333\)" api/src/schemas/species.schema.ts</automated>
  </verify>
  <done>El esquema Zod valida el tamaño del payload y soporta el nivel de peligro 'desconocido'.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar fallback y llamada a Vision API con Logging</name>
  <files>api/src/services/species.service.ts</files>
  <action>
    Instalar `@google-cloud/vision` en `/api`.
    En `SpeciesService`, implementar `identify(imageBase64: string)`:
    1. Limpiar prefijo con `imageBase64.replace(/^data:image\/\w+;base64,/, '')`.
    2. En bloque try/catch:
       - Llamar a Vision API.
       - Extraer labels (mapeo detallado en Plan 02).
       - Si falla (error del SDK o falta de env vars): usar `console.warn('[Vision API Error]', error.message)` y retornar el fallback determinístico:
         `{ classification: 'desconocido', dangerLevel: 'desconocido', name: "Especie Desconocida", description: "La identificación automática no está disponible.", confidence: 0 }`.
  </action>
  <verify>
    <automated>grep -E "console\.warn|replace\(/\^data:image" api/src/services/species.service.ts</automated>
  </verify>
  <done>El servicio maneja errores de Vision API con logging preventivo y retorna un fallback seguro.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar Controlador y Ruta Prioritaria</name>
  <files>api/src/controllers/species.controller.ts, api/src/routes/species.routes.ts</files>
  <action>
    1. En `species.controller.ts`, implementar `identifySpecies`: validar `req.body` con `identifySpeciesSchema`, llamar al servicio y retornar `{ success: true, data: result }` (HTTP 200).
    2. En `species.routes.ts`, insertar `router.post('/identify', identifySpecies)` ANTES de las rutas con parámetros dinámicos (`/:id`).
  </action>
  <verify>
    <automated>grep "router.post('/identify'" api/src/routes/species.routes.ts -c</automated>
  </verify>
  <done>El endpoint está expuesto con prioridad de ruteo y respuesta estandarizada.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Payloads Base64 desde el dispositivo móvil atraviesan hacia el backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | `POST /identify` | mitigate | Validación Zod bloquea payloads > 400KB (533,333 chars). |
| T-23-02 | Information Disclosure | `SpeciesService` | mitigate | Errores de Vision se capturan, se loguean internamente y se retorna un fallback opaco al usuario. |
</threat_model>

<verification>
<validation_steps>
1. Confirmar que `identifySpeciesSchema` tiene el límite `max(533333)`.
2. Verificar que el catch en el servicio usa `console.warn`.
3. Asegurar que `POST /identify` está antes de las rutas `/:id` en el router.
</validation_steps>
</verification>

<success_criteria>
- El backend rechaza imágenes excesivamente grandes (validación en esquema).
- El sistema es resiliente a fallos de la API externa (keys faltantes, cuotas) devolviendo un fallback.
- La respuesta sigue el formato `{ success: true, data: { ... } }`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-ai-identify/23-01-SUMMARY.md`
</output>
