---
phase: 23-backend-ai-identify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/package.json
  - api/package-lock.json
  - api/.env.example
  - api/src/services/species.service.test.ts
  - api/src/controllers/species.controller.test.ts
  - api/src/schemas/species.schema.ts
  - api/src/services/species.service.ts
  - api/src/controllers/species.controller.ts
  - api/src/routes/species.routes.ts
autonomous: true
requirements:
  - AI-01
  - AI-03
must_haves:
  truths:
    - "Dependencias necesarias (axios) instaladas en el backend."
    - "Existen archivos stub para pruebas unitarias de servicio y controlador."
    - "El backend acepta imágenes en base64 para identificación con límite de 400KB."
    - "La API de Vision es llamada de forma segura vía axios con logging de errores (console.warn)."
    - "Existe un fallback determinístico si Google Cloud Vision falla o no está configurado."
  artifacts:
    - path: "api/package.json"
      provides: "Dependencia axios"
    - path: "api/src/services/species.service.test.ts"
      provides: "Stub de pruebas para el servicio"
    - path: "api/src/controllers/species.controller.test.ts"
      provides: "Stub de pruebas para el controlador"
    - path: "api/src/schemas/species.schema.ts"
      provides: "Zod schemas con validación de tamaño (max 533333 chars) y enum DangerLevel.desconocido"
    - path: "api/src/services/species.service.ts"
      provides: "Método identify con supresión de errores y fallback 'cauteloso' per D-11"
  key_links:
    - from: "api/src/routes/species.routes.ts"
      to: "api/src/controllers/species.controller.ts"
      via: "Ruta POST /identify evaluada antes que GET /:id"
---

<objective>
Configurar dependencias, entorno y crear ruta `/identify` con controlador, servicio y fallback robusto.
Purpose: Preparar el terreno técnico y permitir a los astronautas identificar especies mediante fotos (base64) usando Vision API vía REST.
Output: Dependencia axios instalada, stubs de test creados, y endpoint `/api/v1/species/identify` funcionando con fallback determinístico.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/src/routes/species.routes.ts
@api/src/controllers/species.controller.ts
@api/src/services/species.service.ts
@api/src/schemas/species.schema.ts
@api/src/models/species.model.ts
</context>

<tasks>
<task type="auto">
  <name>Task 0: Configuración de Dependencias, Entorno y Stubs de Test</name>
  <files>api/package.json, api/.env.example, api/src/services/species.service.test.ts, api/src/controllers/species.controller.test.ts</files>
  <action>
    1. Instalar axios en el backend: Ejecutar `npm install axios` dentro del directorio `api/`.
    2. Actualizar `api/.env.example` añadiendo `GOOGLE_VISION_API_KEY=`.
    3. Crear `api/src/services/species.service.test.ts` con un stub básico:
       ```typescript
       import { SpeciesService } from './species.service';
       describe('SpeciesService Identify', () => {
         it('should be defined', () => { expect(true).toBe(true); });
       });
       ```
    4. Crear `api/src/controllers/species.controller.test.ts` con un stub básico:
       ```typescript
       describe('SpeciesController Identify', () => {
         it('should be defined', () => { expect(true).toBe(true); });
       });
       ```
  </action>
  <verify>
    <automated>grep "axios" api/package.json && grep "GOOGLE_VISION_API_KEY" api/.env.example && test -f api/src/services/species.service.test.ts && test -f api/src/controllers/species.controller.test.ts</automated>
  </verify>
  <done>Dependencias instaladas, variables de entorno documentadas y archivos de test creados.</done>
</task>

<task type="auto">
  <name>Task 1: Actualizar esquema Zod (Validation & Enum Sync)</name>
  <files>api/src/schemas/species.schema.ts</files>
  <action>
    1. Añadir `'desconocido'` al `dangerLevelEnum` de Zod para sincronizar con el modelo Mongoose.
    2. Modificar `createSpeciesSchema` y `updateSpeciesSchema` para relajar la validación de `imageUrl`: remover `.url()` y permitir `z.string().optional().or(z.literal(''))`.
    3. Crear `identifySpeciesSchema` con:
       `{ imageBase64: z.string().min(1, 'Base64 image is required').max(533333, 'Image too large (max 400KB)') }`.
  </action>
  <verify>
    <automated>grep -E "desconocido|max\(533333\)" api/src/schemas/species.schema.ts</automated>
  </verify>
  <done>El esquema Zod valida el tamaño del payload y soporta el nivel de peligro 'desconocido'.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar fallback y llamada a Vision API vía REST (axios)</name>
  <files>api/src/services/species.service.ts</files>
  <action>
    En `SpeciesService`, implementar `identify(imageBase64: string)` usando EXCLUSIVAMENTE axios (D-05), evitando el uso de SDKs de Google:
    1. Limpiar prefijo con `imageBase64.replace(/^data:image\/\w+;base64,/, '')`.
    2. En bloque try/catch:
       - Llamar a `POST https://vision.googleapis.com/v1/images:annotate?key=${process.env.GOOGLE_VISION_API_KEY}`.
       - Payload: `{"requests": [{"image": {"content": cleanBase64}, "features": [{"type": "LABEL_DETECTION", "maxResults": 10}]}]}`.
       - Extraer labels de `response.data.responses[0].labelAnnotations`.
       - Si falla (error de red o falta de env var): usar `console.warn('[Vision API Error]', error.message)` y retornar el fallback determinístico (D-11):
         `{ classification: 'desconocido', dangerLevel: 'cauteloso', name: "Especie Desconocida", description: "Clasificación no disponible.", confidence: 0 }`.
  </action>
  <verify>
    <automated>grep "dangerLevel: 'cauteloso'" api/src/services/species.service.ts && grep "axios\.post" api/src/services/species.service.ts</automated>
  </verify>
  <done>El servicio usa axios para llamar a Vision API y retorna el fallback 'cauteloso' en caso de error per D-11.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar Controlador y Ruta Prioritaria</name>
  <files>api/src/controllers/species.controller.ts, api/src/routes/species.routes.ts</files>
  <action>
    1. En `species.controller.ts`, implementar `identifySpecies`: validar `req.body` con `identifySpeciesSchema`, llamar al servicio y retornar `{ success: true, data: result }` (HTTP 200).
    2. En `species.routes.ts`, insertar `router.post('/identify', identifySpecies)` ANTES de las rutas con parámetros dinámicos (`/:id`).
  </action>
  <verify>
    <automated>grep "router.post('/identify'" api/src/routes/species.routes.ts -c</automated>
  </verify>
  <done>El endpoint está expuesto con prioridad de ruteo y respuesta estandarizada.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Payloads Base64 desde el dispositivo móvil atraviesan hacia el backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | `POST /identify` | mitigate | Validación Zod bloquea payloads > 400KB (533,333 chars). |
| T-23-02 | Information Disclosure | `SpeciesService` | mitigate | Errores de Vision se capturan, se loguean internamente y se retorna un fallback opaco al usuario. |
</threat_model>

<verification>
<validation_steps>
1. Confirmar que `identifySpeciesSchema` tiene el límite `max(533333)`.
2. Verificar que el fallback usa `dangerLevel: 'cauteloso'`.
3. Asegurar que `POST /identify` está antes de las rutas `/:id` en el router.
</validation_steps>
</verification>

<success_criteria>
- El backend rechaza imágenes excesivamente grandes (validación en esquema).
- El sistema es resiliente a fallos de la API externa (keys faltantes, cuotas) devolviendo un fallback.
- La respuesta sigue el formato `{ success: true, data: { ... } }`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-ai-identify/23-01-SUMMARY.md`
</output>
