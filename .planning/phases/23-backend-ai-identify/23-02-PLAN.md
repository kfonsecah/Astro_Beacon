---
phase: 23-backend-ai-identify
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - api/src/services/species.service.ts
  - api/src/controllers/species.controller.test.ts
  - api/src/services/species.service.test.ts
autonomous: true
requirements:
  - AI-02
must_haves:
  truths:
    - "El mapeo de etiquetas de Vision usa búsqueda de subcadenas (.includes) para mayor robustez."
    - "Existen pases separados para determinar clasificación y nivel de peligro."
    - "La suite de pruebas cubre fallos de configuración (API Key faltante) y errores de red en axios."
  artifacts:
    - path: "api/src/services/species.service.ts"
      provides: "Lógica de mapeo robusta basada en keywords"
    - path: "api/src/services/species.service.test.ts"
      provides: "Tests de unidad para fallos de Vision y mocks de etiquetas multi-palabra"
    - path: "api/src/controllers/species.controller.test.ts"
      provides: "Tests de validación de tamaño de payload (400KB limit)"
  key_links:
    - from: "api/src/services/species.service.test.ts"
      to: "api/src/services/species.service.ts"
      via: "Mocks de axios.post retornando etiquetas como 'Flowering plant' mapeadas correctamente a 'planta'"
---

<objective>
Refinar mapeo de etiquetas (keywords) y robustecer suite de pruebas mockeando axios.
Purpose: Asegurar que el sistema identifique correctamente categorías incluso con etiquetas descriptivas largas y sea resiliente a errores de entorno.
Output: Lógica de clasificación robusta y cobertura de pruebas completa para escenarios de éxito y fallo (mockeando REST).
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/Astro_Beacon/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/src/services/species.service.ts
@api/src/services/species.service.test.ts
@api/src/controllers/species.controller.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lógica de Mapeo por Keywords y Doble Pase</name>
  <files>api/src/services/species.service.ts, api/src/services/species.service.test.ts</files>
  <behavior>
    - Una etiqueta 'Flowering plant' debe detectarse como 'planta' mediante `.includes('plant')`.
    - Realizar un pase para `classification` y otro independiente para `dangerLevel` (no interrumpir el bucle prematuramente).
    - Keywords: 'plant'/'tree' -> planta, 'animal'/'mammal'/'bird' -> animal, 'mineral'/'rock'/'water' -> recurso.
    - Peligro: 'predator'/'carnivore'/'danger' -> peligroso.
    - Los tests deben mockear `axios.post` para retornar estructuras de respuesta de Google Vision.
  </behavior>
  <action>
    1. En `species.service.ts`, refactorizar la lógica de `identify` para iterar sobre las etiquetas extraídas de la respuesta de Vision.
    2. Usar `.toLowerCase().includes()` para comparar etiquetas con keywords.
    3. Implementar pases separados: primero buscar clasificación, luego buscar indicios de peligro.
    4. Actualizar `species.service.test.ts` con casos de etiquetas multi-palabra mockeando `axios.post`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=species.service.test.ts</automated>
  </verify>
  <done>La clasificación es precisa frente a etiquetas variadas y el nivel de peligro se evalúa independientemente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pruebas de Fallo y Resiliencia</name>
  <files>api/src/controllers/species.controller.test.ts, api/src/services/species.service.test.ts</files>
  <behavior>
    - Si `axios.post` falla (red o 4xx/5xx), se activa el fallback.
    - Si se envía un string base64 > 533,333 caracteres, el controlador retorna 400.
    - Errores de red o cuota en axios retornan el fallback con `confidence: 0` y `dangerLevel: 'cauteloso'`.
  </behavior>
  <action>
    1. En `species.service.test.ts`, añadir pruebas que simulen `axios.post` lanzando errores o retornando respuestas vacías.
    2. En `species.controller.test.ts`, añadir un test que envíe un payload excesivamente grande y verifique el error de Zod.
    3. Asegurar que las pruebas verifiquen la presencia de `console.warn` (spyOn).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=species.controller.test.ts|species.service.test.ts</automated>
  </verify>
  <done>Todos los escenarios de fallo críticos (configuración, tamaño, API) están cubiertos por pruebas mockeando la red.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Registry | Fallos en la inicialización de servicios externos inyectados o configurados por env vars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Availability | `SpeciesService` | mitigate | El fallback determinístico se activa incluso si la API Key es inválida o hay errores de red. |
</threat_model>

<verification>
<validation_steps>
1. Ejecutar tests y confirmar que 'Flowering plant' -> 'planta'.
2. Confirmar que el envío de 600,000 caracteres base64 falla en el test del controlador.
3. Verificar que el fallo simulado de axios devuelve el fallback con `dangerLevel: 'cauteloso'`.
</validation_steps>
</verification>

<success_criteria>
- El mapeo no es binario (exacto) sino heurístico (keywords).
- La suite de pruebas garantiza que el sistema no "muere" sin credenciales o con errores de red.
- Límites de tamaño de imagen están validados por tests.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-ai-identify/23-02-SUMMARY.md`
</output>
